name: CI

# Production safety gate (RC1 P0-1). Runs on every push + PR. Blocks a red build from being
# merged/relied on. Pair with GitHub branch protection (require this check on `main`) — that
# settings step is owner-side (see CREDITVECTOR_RC1_EXECUTION.md P0-1).
on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate-d-preflight:
    name: Gate D preflight
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      NEXTAUTH_SECRET: ci-placeholder-not-used-in-production
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - name: Install (lockfile-exact)
        run: npm ci
      # Keep these DB-free release-security checks ahead of typecheck. If a
      # framework migration breaks compilation, CI must still record whether
      # dependency custody, agency authority, PWA caching, and verifier
      # contracts passed on the exact installed graph.
      - name: Release-security deterministic guards
        run: |
          set -euo pipefail
          for guard in \
            dependency-runtime \
            agency-enable-security \
            pwa-cache-policy \
            verify-production
          do
            npx --no-install tsx "scripts/${guard}.test.ts"
          done
      - name: Gate D deterministic verification
        run: |
          set -euo pipefail
          npx --no-install prisma --version
          npx --no-install tsx scripts/gate-d-preflight.test.ts
          npx --no-install tsx scripts/release-verify.test.ts
          npx --no-install tsx scripts/schema-safety.test.ts
          bash -n scripts/release-verify.sh
      - name: Typecheck
        run: npm run typecheck

  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      # CI-only placeholder so build-time auth helpers never see an undefined secret. NOT a real
      # secret and never used in production.
      NEXTAUTH_SECRET: ci-placeholder-not-used-in-production
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - name: Install
        run: npm ci
      - name: Guards (DB-free suite)
        run: |
          set -e
          for f in scripts/*.test.ts; do
            case "$f" in
              *classify.test.ts) echo "skip $f (requires a database)"; continue;;
            esac
            echo "── $f"
            npx --no-install tsx "$f"
          done
      # scripts/*.test.ts is a NON-RECURSIVE glob, so the runtime guards under
      # scripts/runtime/ were never executed by the step above — the strongest
      # money-path evidence we have was not evidence CI ever looked at. These run the
      # real route handlers rather than matching their source text. (Guards that
      # execute code but live directly under scripts/ are already covered by the step
      # above; only this directory needs its own step.) run-all.ts globs its own
      # directory, enforces an explicit REQUIRED list, and fails the job on any failure.
      - name: Runtime guards (real handlers, mocked Stripe and Prisma)
        run: npx --no-install tsx scripts/runtime/run-all.ts
      - name: Typecheck
        run: npm run typecheck
      - name: Lint (advisory — pre-existing issues; not a launch gate)
        run: npm run lint
        continue-on-error: true
      - name: Build
        run: npm run build
