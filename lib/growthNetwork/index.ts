// Client-safe public Growth Network foundation. Environment flags are deliberately not
// re-exported; server callers must explicitly import `./flags` after future approval.
export * from "./foundation";
