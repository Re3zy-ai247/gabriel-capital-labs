# Current State

> Authoritative snapshot. Update after meaningful work; keep compact; move superseded detail to `ARCHIVE/`. Never turn this into a chronological log.

**Last updated:** 2026-07-14 · **Branch:** `main` @ `a4e1204` + Sprint XII working tree (uncommitted, not pushed) · **Prod:** LIVE at https://www.creditvector.app (Vercel auto-deploys on push to `main`)

**Recent (2026-07-12, docs-only, uncommitted):** Phase 1 governance layer (`.ai/` constitution + canonical docs + ADRs), **Phase 2 CVIOS** (`VISION.md`, `CVIOS.md`, `executive/` ×10, `business-intelligence/`, `marketing/`, `knowledge/`, `improvement/`), **Phase 3 Kai Intelligence design** (`KAI-INTELLIGENCE.md` + `CREDIT-ECONOMY.md` + ADR-0006 Proposed), and **Phase 4 Kai Experience design** (`KAI-EXPERIENCE.md` + ADR-0007 Proposed — passive/active split, event engine, Kai Home/Timeline/Digest, notification voice, brand) installed, plus the **Creative OS** (`creative/` — Kai Character Bible [now PERMANENT with IP clause], cinematography/motion bibles, Higgsfield prompt blocks, storyboards, consistency-scoring gate, **Kai product-design spec** (`creative/KAI-PRODUCT-DESIGN.md` — design language, landing/product UX, behavioral states, conversation UX, projection language) and a 50-concept marketing library; ADR-0008 Accepted), and the **Brand Universe layer** (company-level: `creative/BRAND-UNIVERSE.md` + Rey↔Kai dynamic + cinematic bible + Media OS + hologram system + founder story [facts DRAFT — founder to confirm] + production pipeline; Kai elevated to GCL-level IP, "mascot" vocabulary retired). Zero code changes. Runtime awaits founder approval; recommended first builds = AI-cost metering (BI-COST-01) and/or E1 event engine + timeline. **Creative next step: founder uploads the 4 Kai reference photos via the pending Higgsfield widget → generate + score the CV-KAI-MASTER-001 candidate (~1,010 credits available; stills-first).**

## Production status
- Stripe **LIVE** (`sk_live`), webhook verified; catalog synced; letter-pack credits idempotent.
- Resend email live from verified domain `creditvector.app` (deliverability still warming up).
- Anthropic AI live (Opus 4.8 via `LLM_MODEL`); daily Brief-ingest cron + weekly digest cron scheduled (`vercel.json`), `CRON_SECRET` set.

## Working major modules (all VERIFIED shipped)
Report upload → AI parse/analyze → tradelines/scoring → strategist → dispute letters (+ round 2, response analysis) · Community Hub + Kai · Support tickets · Encrypted attachments & identity documents · Agency workspaces · Billing/entitlements · **Brief** (news feed — Phases 1–3 complete: publish flow, likes/bookmarks/shares, post-moderated comments, RSS+PDF news automation with draft-only ingestion, YouTube embeds, weekly digest built) · Admin dashboards (Overview+churn, Product Health, Marketing, Automation — AIOS Phase 4 complete) · Web Push · Password reset.

## ⏰ Pending OWNER actions (raise at session start)
1. **`COMPANY_POSTAL_ADDRESS` env var** — weekly Brief digest is built & live (`e699eb6`) but sends NOTHING until this is set in Vercel prod (CAN-SPAM footer address). Then: owner subscribes in Settings → "Send test digest" from `/admin/brief` → verify.
2. **One-time backfill** — admin console: `fetch('/api/admin/encrypt-letters',{method:'POST'})` (encrypts pre-existing plaintext Letter rows; idempotent). Also `/api/admin/encrypt-reports` if never run. Status: NEEDS CONFIRMATION whether either has been run.
3. **Counsel sign-offs required** (Article IV): (a) CROA "educational, not credit-repair" positioning; (b) news-editorial/defamation posture **before publishing the FIRST auto-drafted Brief article** (drafting/queuing is fine).
4. User-side Stripe emails: enable Customer emails → Successful payments; merchant-notification email.
5. Owner test pending: Brief comment flow (post → banned phrase → 422; report → moderate at `/admin/brief/comments`).

## Known issues / debt
- Favicon/OG images still the pre-de-shadow shield render (on-brand; regen pending).
- Digest should add a `List-Unsubscribe` header when it starts sending.
- `.env.example` drift: still lists deleted `SETUP_SECRET`; missing `COMPANY_POSTAL_ADDRESS`, `CRON_SECRET`.
- `tsconfig.tsbuildinfo` is tracked in git (build artifact).

## Build Era — Sprint 1 SHIPPED to working tree (2026-07-12, uncommitted→committed locally, NOT pushed)
**Roadmap #4 BI-COST-01:** `lib/aiMeter.ts` — `meteredMessage()` now wraps ALL 9 Anthropic call sites (kai, parse, brief-summarize, response-analysis, strategist, identity×2, letter-generate, letter-round2); `AiUsage` self-heal table records tokens/cost-estimate/latency/success per call, fail-open; this is also the ADR-0009 provider seam. **Roadmap #5 ADR-0007 E1:** `lib/kaiEvents.ts` — `KaiEvent` self-heal table + fail-open `recordKaiEvent()`/`listKaiEvents()`; producers live in upload (uploaded+analyzed), re-analyze, letter-generate, letter PATCH (mailed + dispute.resolved), letter response (response.received). Validated: tsc clean · next build clean · guards green (brief-ingest 26/26, kai-sanitize 8/8, classify 29/29). **Product Sprint 1 (same day): Kai Home + Timeline SHIPPED to tree.** `lib/kaiHome.ts` (passive rules: overnight delta, one recommendation with receipt, §611 deadline radar) · `app/dashboard` → Kai Home (KAI chip greeting, KAI RECOMMENDS card, radar with progress bars, real responses-received stat replacing the "~ estimate" card [CX-2], timeline snippet) · `app/journey` → THE timeline (live events + deduped synthesized history + upcoming windows + kept 90-day checklist [CX-3]) · Sidebar: "Kai Home"/"Timeline". All server-rendered, zero AI calls, zero new JS. Validated tsc+build. **Sprint 1 pass 2:** Kai Response Cards live on letters (outcome-aware teaching per §611/§605 receipts, evidence + weaknesses, ONE next action, honest basis line, deleted = quiet celebration) · Floating Kai presence (`components/kai/KaiPresence.tsx` + `/api/kai/context` — session-cached passive context, never auto-opens, session-dismissible, hidden on Kai Home) · Kai working-states replace generic spinners (upload/letters: "Kai is reading…/drafting…"). Validated tsc+build. **Kai asset system:** `/assets/kai/` tree (9 folders) + `manifest.json` version ledger (full metadata schema; never-overwrite-canonical + parent-version laws) + guard `scripts/kai-manifest.test.ts` (44 checks green) + `lib/kaiStates.ts` (16-state emotional catalog, typed, asset paths wired for drop-in; 3 renders still needed: Concerned/BadNews/Listening). CV-KAI-MASTER-001 = Kai v1 canonical (founder-approved; deltas recorded). **Proactive-Kai sweep (14 files, workflow-built + adversarially verified + hand-fixed):** strategist/tradelines/scores/support/help/settings/community/brief/letters/agency reframed to proactive Kai voice with REAL-data claims only; letters rows carry §611 window storylines; Agency Command Center v1 (Kai briefing card + priority sort with reasons); `/admin/automation` now renders measured BI-COST-01 AI cost per surface. Verifier findings fixed pre-commit: fabricated community-moderation claim removed, "reads every thread" → "bring me in", Brief sources stated truthfully, §611 clock phrased from bureau RECEIPT not mailing, identity-freeze governance recorded (DECISIONS ADR-0008 amendment). **Experience fleet (8 files + verifier fixes):** Landing V2 (trust pledge ×2, Kai hero intro with reserved portrait path, truthful DashboardPreview with KAI RECOMMENDS mock, hype cut) · Kai Home/Timeline mobile+motion+progressbar-a11y pass · letter PRINT ELEVATION (typeset @page US-Letter document; footer is SCREEN-ONLY — mailed artifact stays unbranded pending CCO) · AiPlan token/voice fix · shell a11y (aria-current, 44px targets, KaiPresence non-modal dialog with safe focus handoff). Verifier-driven fixes applied: statute-promise softened, 48h-window copy truthed, DashboardPreview clock claim aligned to product, KaiPresence focus bugs (dismiss-focus/navigation-yank/Escape-steal) fixed. Auditor HIGHs fixed: skip-link target (main id), theme-aware focus ring, letters row wrap @375px, emerald/blue→success/ocean tokens, upload textarea label. **Auditor items DEFERRED (logged):** landing force-dynamic→middleware (perf, needs auth-flow care), Sidebar duplicate context fetches → provider, letters/upload server-prefetch, letters form htmlFor/id pass, logo-mark resize. Remaining sprint: state-render batch, community/agency continued, deferred perf items.

**Product Sprint 2 (2026-07-12):** Roadmap **#28 tradelines truth surfacing** — new `lib/tradelineInsights.ts` (display math over the EXISTING engines: `lib/obsolescence.ts` + `Tradeline.duplicateGroup`; invents nothing — no DOFD → no claim) + `app/tradelines` elevation: §605 fall-off countdown chips (D15/W4: "falls off ~Mar 2027", "window passed"), duplicate-debt grouping with adjacency sort + "same debt ×N" badges (W5), NOT_RECOMMENDED rows now say "set aside" with the why (W6), Kai card gains duplicates + set-aside findings; guard `scripts/tradeline-insights.test.ts` (9/9). Roadmap **#44 scores arc view** — per-bureau "+N since Mar 2026" first→latest deltas on the latest cards (success/rose/slate by sign), chart series moved onto palette tokens (brand/ocean/gold — emerald removed per green-only-success law), theme-aware gridlines/labels (`stroke-ink-700`/`fill-slate-500`), D96 line draw-in via existing `.animate-draw` (reduced-motion safe), chart `role="img"` + honest aria description. Roadmap **#21 empty-states audited: already covered** by the Sprint-1 proactive sweep — no changes needed. Hygiene: `.env.example` adds `CRON_SECRET`+`COMPANY_POSTAL_ADDRESS` + SETUP_SECRET delete-after-bootstrap warning (kept — still a live code path, TASKS corrected); stale TASKS items cleared (`List-Unsubscribe` already shipped in `lib/briefDigest.ts` incl. One-Click; tsbuildinfo already untracked). **Pass 2 — Roadmap #14/W2 "this account appears N ways":** every tradeline row with real per-bureau field data is now a native `<details>` disclosure (server-rendered, zero client JS) opening a side-by-side bureau comparison — status/balance/reported/DOFD/remarks per bureau, conflicting fields highlighted gold with a hover why, explicit-ABSENT bureaus shown as "not reporting" (unknown stays silent), Kai states the real conflict sentences + the §611 inconsistency angle. `lib/bureauData.ts` refactor: new `conflictFields()` powers both the highlights and `crossBureauConflicts()` (sentence text asserted unchanged in the guard, now 12/12). **Pass 3 — Roadmap #15/W13 statute card library:** `components/StatuteCard.tsx` (plain-English desc + the real operative text one disclosure away; no client JS) · `/help` gains "The law, in plain English" (all 7 vetted statutes from `lib/statutes.ts`, content untouched) · tradelines gains contextual cards — ONLY the statutes the user's rows actually invoke (§611 if anything disputable, §605 if flagged, FDCPA §809 if collections). **CCO gate ran: GO-WITH-CHANGES, change applied** — §605 chip rephrased from "falls off ~Mar 2027" (removal prediction) to "§605 window ends ~Mar 2027" (statutory fact); 2 Low findings tracked (keep "argument" not "removal" in hovers; never reuse scores deltas in marketing without results-vary framing). **Pass 4 — G-14 RESOLVED:** `liveMrrCents()` in `lib/stripe.ts` sums real Stripe subscriptions (active+past_due, monthly-normalized, trials/comped excluded; fail-open null) · `/api/admin/overview` serves real MRR with the counts×price estimate only as fallback + `mrrSource` field · admin UI labels the stat "MRR (Stripe)" vs "MRR (estimated)" with an honest footnote per source · dead duplicate `/api/admin/stats` route DELETED (zero consumers — admin page fetches only `overview`) · stray `text-emerald-400` → `text-success-400` token. Skipped-with-reason: roadmap #9 notification voice templates+caps — no user-facing push producers exist yet (push is admin-only), so templates now would be a speculative abstraction; build alongside E3 digests/first user-facing producer.

**Build Wave 3 (2026-07-12, founder directive: premium finish, push after validation):** **Batch 1 speed** — `useAdminContext`/`useCommunityAccess` gained a module-level promise cache (60s TTL): ONE `/api/admin/context` + ONE `/api/community/access` per page load instead of up to 4 concurrent duplicates (Sidebar, MobileNav, ImpersonationBanner, AdminTabs); hook contracts unchanged. Landing signed-in redirect moved to new `middleware.ts` (edge `getToken`, matcher `["/"]` only) — `force-dynamic` removed, `/` now prerenders **static**. Letters/upload server-prefetch stays deferred (restructures under CX-1; prefetch now = rework). **Batch 2 letter intelligence** — mailed rows carry the §611 progress bar (same visual grammar + a11y as the Kai Home radar: gold in-window, rose overdue, "Nd left"/"window passed"), "Mark mailed" emphasized as THE next action with a why-tooltip (starts the clock). **Batch 3 language** — product-wide transport-error voice unified to the scores-page standard ("The connection dropped mid-request. Try again — nothing was lost.") across 16 sites; "Failed"/"Generation failed" → calm human sentences; bare "Loading…" on settings/billing/documents → contextual lines ("Pulling up your account…"); upload's "report(s)" → real pluralization. Validated: tsc clean · build clean (48/48, `/` static, middleware 47.4kB) · 11 guards green.

**Production hardening (2026-07-12, post-incident):** incident triage confirmed NO outage — the reported 500 was Next.js deployment skew (stale Server Action from a pre-June tab; 1 hit/24h, self-heals on refresh; zero 5xx in 24h of prod logs). Hardening shipped (`66c9692`): **`x-cv-release` header** (12-char commit SHA baked at build in `next.config.js`) on every response — triage rule: stale SHA = skew, current SHA = real bug; **mutation throw-safety audit** — 26 busy-flag sites, 8 stranded a spinner on thrown fetch (login/register/settings/reset-password/upload-reanalyze/agency-openClient/admin×2), all fixed with transport guards; **Server-Action audit: codebase has ZERO server actions** (all mutations = API routes) — rule recorded: never add one without skew protection; **Vercel Skew Protection BLOCKED on Hobby plan** (API-verified `invalid_billing_plan`) — ⏰ OWNER decision: upgrade to Pro (~$20/mo) to enable; checklist + middleware/auth compat pre-check in root **`OPERATIONS.md`** (new: deploy protocol, incident triage, CDN cache contract [verified: static revalidates per-deploy via etag, hashed assets immutable-1yr, dynamic no-store], rollback procedure; `.ai/RUNBOOKS/deploy.md` cross-links it).

**Sprint XV — Credit Intelligence Platform / CVI (2026-07-14, engines→platform; ADR-0015; orchestration only, MAIL_LIVE stays OFF):** every intelligence capability now sits behind ONE internal API so future products (Funding Hub, Credit Builder, Business Credit, Monitoring, Mobile, AI API) ask the platform once and never compute their own intelligence. **`lib/intelligence/`**: `snapshot.ts` `loadSnapshot(userId)` reads the case ONCE (reusing Outcome Ledger, §611 clock, Kai events, scoring/account data) → immutable snapshot; `modules.ts` = the nine modules as PURE functions (Credit Profile, Credit Health, Risk Analysis, Opportunity Engine, Timeline Intelligence, a parameterized **Readiness** engine [mortgage/auto/personal/credit-card/rental/employment + business], Builder Intelligence, Business Readiness); `types.ts` = the shared envelope (summary·metrics·reasons·recommendedActions·confidence·history·nextSteps); `api.ts` = pure `assembleIntelligence(snapshot)` + `creditIntelligence(userId)`; `/api/intelligence` (userId-scoped). **Mission Control CONSUMES the platform** via `ReadinessStrip` on `/dashboard` (renders platform-computed readiness, computes nothing itself — the pattern every future module follows). **Honesty over fabrication:** the parsed report has NO credit limits → utilization + detailed payment history disclosed "Not tracked"; absent data → `confidence:"insufficient"`; business readiness = honest scaffold (no external connections). **CROA/FTC:** readiness = whether the file is in shape for a goal, explicitly NOT a lending decision/approval prediction. Guard `scripts/intelligence-platform.test.ts` (16 assertions incl. determinism, no-report edge, utilization-not-tracked, readiness-responds-to-signals, CROA/approval-odds forbidden-phrase sweep). Validated: tsc clean · next build clean (`/api/intelligence` live) · **21/21 guard scripts green**. **Five-lens review ran (Architecture/Legal/Privacy/Adversarial+Determinism/Accessibility, 16 agents, 9 confirmed) — the High + all Mediums + cheap Lows FIXED:** (HIGH, adversarial) collections/charge-offs/public-records counted RESOLVED items while negatives/disputable excluded them → self-contradicting profile/health/risk/readiness on the win-a-dispute happy path — now all resolved-EXCLUSIVE (`active()` helper); (MED, arch+legal) §605 obsolescence was re-derived inline with a bare `7` (ignoring the §1681c(c)(1) 180-day offset + 10-yr bankruptcy window) → now reuses the canonical `fallOffInsight`, and the "eligible for removal now" copy → factual "past its §605 reporting window — a clean obsolescence ground"; (MED, adversarial) open-window filter aligned to `status==='MAILED'` (no phantom overdue from self-resolved letters); (MED, a11y) readiness chips → colored-dot + neutral slate label (theme-robust contrast, color never the sole signal); (LOW) risk reasons now severity-sorted so "top signal" is the worst; readiness timeline copy → "until your current response windows close (§611)" (no implied favorable date); §611-reuse comment corrected. Privacy: 0 findings (userId-scoped throughout). Backlog Low: dashboard loads Mission Control + platform snapshot separately (unify next). Guard gained a risk-severity-order assertion. Re-validated: tsc + build clean, 21/21 green. Stacked on XII–XIV; preview only, no merge.

**Sprint XIV — Verified Outcome Ledger (2026-07-14, closes the compounding loop; ADR-0014; deterministic, MAIL_LIVE stays OFF):** the moat substrate — every logged bureau response now becomes ONE canonical verified-outcome row that learns. **`lib/outcomeLedger.ts`**: self-heal `VerifiedOutcome` table (ADR-0001) keyed by `letterId` (upsert → never double-counted), linking the dispute (strategy·recipient·bureau·round·accountType·isDebtBuyer) AND the recommendation that produced it (`decisionId`, resolved from Decision Registry) to outcome + latency + provenance. **Write** at `/api/letters/[id]/response` (fail-open) + a one-time idempotent backfill of historical responded letters (`ON CONFLICT DO NOTHING`). **OWN-data feedback (gate-free):** `ownOutcomeTrack`/`ownHistorySummary` → Mission Control Command Center "Your history" line (own data, ≥3-response threshold, history-not-prediction per CROA — reuses `lib/forecast` framing). **Cross-user (unchanged gates):** `ledgerCorpus` yields ONLY consenting users' rows (`consentingUserIds`), identifier-stripped (`userId`→salted HMAC key for downstream distinct-contributor k-anon only), aggregated by Engine 1 `outcomeStats` (K_ANON_MIN=20), consumer display still CCO-gated (ADR-0010). Reuse over new engine: outcome vocabulary + consent + k-anon + aggregation shape all reuse Engine 1. Decision Registry (proves the recommendation) + Verified Outcome Ledger (proves the outcome) = the end-to-end verified pair — substrate for a future Credit Intelligence API. Guard `scripts/outcomeLedger.test.ts` (pure `computeOwnTrack`/`ownTrackLine`/`ownHistorySummary`/`toOutcomeRecord` incl. identifier-stripping + history-not-prediction assertions). Validated: tsc clean · next build clean · **20/20 guard scripts green**. ⏰ Required reviews (Legal/Privacy/Adversarial/Accessibility) — running. Fast-follow (deferred per token discipline): wire own-history into `recommendationIntel`/`forecast` panels; swap the dormant `buildOutcomeCorpus` to `ledgerCorpus` once the CCO gate opens. Stacked on the XII/XIII branch.

**Sprint XIII — Mission Control (2026-07-14, the operating dashboard; ADR-0013; orchestration only, no new engine, MAIL_LIVE stays OFF):** the post-login `/dashboard` is now **Mission Control** — it tells the customer what to do every day by ORCHESTRATING the existing engines (never duplicating them). **`lib/missionControl.ts`**: a **pure** `assembleMission(inputs)` (guard-tested, no DB) + a thin `getMissionControl` loader that reuses `getKaiHomeData` (recommendation/deadlines/overnight/events), `caseMemorySince`, the Campaign service/composer/policy, the §611 clock, and scores. It composes: **Today's Mission** (deterministic checklist — escalate response / upload lapsed response / mail approved campaign / review recommended campaign / upload report), **Waiting On** (running §611 windows), **Happening Automatically** (staged accounts + clock tracking), **Kai's Next Action** (single, priority-ranked, with its receipt), **Campaign Capacity** (recommended size + reasons + auto-staged remainder — **operational guidance, never a legal claim**), **Deferred Queue** (why deferred / what unlocks / est. review date from the real §611 window / dependency — no fabricated dates), the **8-section Command Center** (Campaigns/Mail/Responses/Timeline/Score-progress/Deadlines/Deferred/Next — all deep-linked, tone-coded), and a **5-signal Health Dashboard** (Campaign/Mail/Response/Timeline + Case roll-up green/amber/red). Components `components/mission/MissionControl.tsx` + `CommandCenter.tsx` (presentational server components; nothing static). Sidebar "Kai Home" → "Mission Control". The old Kai Home cards are subsumed (greeting + Case Memory reused; deadline radar/quick-wins/stats fold into the Mission Card + Command Center). Guard `scripts/missionControl.test.ts` (32 assertions incl. on-track/upload/overdue-red/waiting/approved-unmailed/pending-review/composed-capacity/deferred-est-date/score-delta + operational-not-legal capacity copy + M4 task↔health agreement + M5 urgent-outranks-mail). **Five required reviews ran (Architecture/UX/Accessibility/Legal/Adversarial, 31 agents, 20 confirmed) — all 5 Mediums + high-value Lows FIXED:** (M-adversarial) next-action no longer ranks "mail a new campaign" above an overdue/escalation red state (urgent kaiHome recommendation now outranks it); (M-adversarial) response-health + escalate-task now share ONE escalatable set so they can't disagree on `updated`/`unknown` outcomes; (M-legal) capacity reason "Better investigation quality" (an outcome claim about the CRA's process) → operational "easier for you to document and track"; (M-a11y) load-bearing `text-slate-500` → `slate-400` (was <4.5:1 on dark) and section titles promoted to real `h3`/`h4`; plus dead `gold-300/200` tokens → `gold-400` (amber text had no color — also fixed in the XII campaigns/mail-send pages on this branch), overdue/awaiting counts now derived from letters (robust past Kai Home's 6-window cap), deferred est-date suppressed when the window has passed (no past dates), self-linking "next" tile → /journey, Command Center hidden for cold-start, first-time greeting no longer says "Welcome back". Backlog (Low, non-blocking): DRY the LIVE_CAMPAIGN set with the campaign engine; dedupe getMissionControl's row reads; the single next step appears in 3 framings (mission row / hero / tile) by design; tiny "view" link <44px. Re-validated: tsc clean · next build clean · **19/19 guards green**. Built on the Sprint XII branch (Mission Control consumes the Campaign engine), ships stacked with Sprint XII.

**Sprint XII — Kai Campaign Intelligence (2026-07-14, deterministic focused dispute sequencing; ADR-0012; MAIL_LIVE stays OFF):** the product no longer just asks "which accounts?" — it answers "what's the clearest, most evidence-supported campaign to pursue next, and why?" New **`lib/campaign/`** (pure, mirrors `lib/mail/`): **`CampaignModel`** (10-state machine DRAFT/RECOMMENDED/NEEDS_REVIEW→APPROVED→ACTIVE→…→COMPLETED +CANCELED/SUPERSEDED; items = included/deferred/excluded each with a reason; **immutable snapshot frozen at approval**; warnings CATEGORIZED law/policy/recommendation) · **`CampaignComposer`** (deterministic, **NO AI/no hidden reasoning** — ranks by evidence-of-GROUNDS not outcome, leads with the strongest item's strategy family [bureau §611 / obsolescence §605 / furnisher §623 / collector §1692g], includes up to the policy's recommended max, defers the rest with reasons + next-unlock; EXCLUDES not-recommended/government, DEFERS active-investigation [no duplicate reinvestigation] + already-in-flight; `reviewSelection` = Strategy Review with conflicts/families/weak-items/recommended-split; **expanded override ALWAYS available, never a silent block**) · **`CampaignPolicy`** (**operational NOT legal** — recommendedMax 5 / warn 6 / hard-ceiling 12 / per-recipient 8, env-overridable `CAMPAIGN_POLICY`, version stamped into each snapshot = audit history; NO copy claims a count is frivolous/illegal) · **`CampaignStore`** (self-heal `Campaign` table ADR-0001 + in-memory; snapshot write-once + audit append-only store-enforced; every query userId-scoped = tenant/agency-client isolation) · **`CampaignService`** (compose/create/approve[freeze]/cancel + **the mail gate**). **Mail GATE (Phase 7):** `/api/mail/[mailId]/confirm` now requires every queued letter to belong to an approved campaign covering it; if none, it lazily creates+approves a **coherent single-item campaign** (a lone send is never blocked); fail-closed on association error (nothing queued). **Surfaces:** `/campaigns` (Kai's recommended campaign + deferred/excluded reasons + Strategy Review + expanded override with recorded acknowledgment) · Sidebar "Campaigns" + letters entry point · campaign milestones as fail-open KaiEvents (`campaign.approved/active`) → Timeline + Case Memory · Decision Registry records the acted-on campaign at queue time. Guard `scripts/campaign.test.ts` (45 assertions incl. lead-family, active-investigation deferral, weak cap, size verdicts, input-order determinism, snapshot per-CRA coverage, **null-tradeline gate**, tenant isolation, gate single-item creation, CROA/frivolous-phrase sweep). **Two required reviews ran: (1) CCO gate GO-WITH-CHANGES — the one Medium fixed** (single-item auto-campaign rationale de-claimed for FTC substantiation); **(2) adversarial review (5 lenses × per-finding verify, 20 agents, 10 confirmed) — ALL actionable ones fixed:** the **fail-OPEN gate gap** (a mailable letter with a null `tradelineId` — a personal-info bureau correction, or a dispute whose tradeline was deleted via `onDelete:SetNull` — skipped the gate and queued campaign-less) now gates fail-closed via a `letter:<id>` dispute key; composition made env-portable (code-point ordering, sorted excluded/deferred sections); empty-`bureaus[]` snapshot now covers any CRA for its tradeline; `letterTarget`'s tradeline read userId-scoped; markQueued made retry-idempotent (no redundant audit). Validated: tsc clean · next build clean (`/campaigns` + 2 API routes) · campaign (45) + mail-execution guards green. ⏰ **CCO/counsel review of the consumer-facing campaign + §611(a)(3) frivolous-framing language before push** (compliance-review gate). Backlog (Low, MAIL_LIVE off): (a) concurrent-confirm find→create + nextSequence races can mint a duplicate/parallel single-item campaign or dup sequence number — add a `(userId,sequence)` unique/idempotency key; (b) a multi-CRA bureau item records only the first per-CRA `letterId` (all pass the gate; tracking is partial); (c) admin policy-editor UI for `CampaignPolicy`; (d) agency per-client campaign command view (composer/store already per-client-isolated).

**Sprint XI — Verified Mail Execution (2026-07-14, the workflow completes; MAIL_LIVE stays OFF):** the customer can now execute a dispute end-to-end inside CreditVector — recommendation → review → approve → confirm → **QUEUED** — without a single real letter moving. **New flow** `/mail/send/[letterId]`: (1) Approval Review (recipient/address/tradeline/round/PDF-preview link/mail class/postage/pages/expected-mailing/legal disclaimer), (2) Payment Review (real pricing from the engine — platform fee/provider/markup/discount/coupon; certified+taxes as reserved placeholders; **honest banner: no card charged, no letter sent while MAIL_LIVE off**), (3) Manifest receipt (manifest ID, generated time, recipient, **letter content hash = proof of intent**, print spec, status, append-only audit). Wired through Sprint VIII `MailService`: `initiate`→`approve`(user only)→`markPaid`(no-charge ref)→**new `queueForProvider`** (PAID→QUEUED and **STOPS** — no provider contacted, no provider state ever fabricated; `dispatch` stays gated by MAIL_LIVE for Sprint XII). Helpers `lib/mailExecution.ts` (deterministic page estimate, SHA-256 content hash, structured bureau/furnisher/sender addresses — never invented). Entry points on letters ("Mail with CreditVector" primary + "Mark mailed myself"); **Mail Center** joins real manifests (mailId=`mail_<letterId>`) showing "CreditVector Mail · Queued", provider placeholders now "Available after live mail integration"; **Timeline** renders mail.status milestones (recommended/approved/paid/queued) in Kai's voice; **Case Memory** surfaces the queued milestone; **Decision Registry** records the acted-on recommendation on confirm. Guard `scripts/mail-execution.test.ts` (20 assertions incl. "never advances past QUEUED / no provider job id"). ⏰ Sprint XII (deferred): flip MAIL_LIVE, wire LetterStream HTTP + real payment + certified + tracking + delivery + failover/retry/health — needs CSO + CCO sign-off before any physical mail.

**Sprint X — Credit Intelligence Engine I (2026-07-14, deterministic, ADR-0010 amendment):** the compounding Outcome Intelligence engine — NO AI, NO prediction, only counts over real outcomes. **Engine 1** (`lib/outcomeStats.ts`): deterministic aggregation of dispute outcomes into k-anonymous patterns (favorable rate / median latency by strategy/bureau/round/account-type), **K_ANON_MIN=20** — no cell reports a rate/median below threshold (thin data discloses itself). **Engine 2** (`lib/recommendationIntel.ts`): own-data recommendation intelligence — why-this-strategy/recipient/now, alternatives, expected timeline (recipient-correct statute), next step, **case confidence in the GROUNDS not the outcome** — shipped to consumers gate-free as `RecommendationIntelPanel` in the tradelines expander. **Decision Registry** (`lib/decisionRegistry.ts`): append-only recommendation ledger (infra, fail-open, not render-wired). **Consent surface** (`lib/outcomeConsent.ts` self-heal `OutcomeConsent` + Settings toggle, opt-in only) = the MOAT-rule-2 precondition for cross-user aggregation. **`lib/outcomeCorpus.ts`**: assembles the corpus from ONLY consented users' letters, **stripped of every identifier** (no userId leaves the function). **`/admin/intelligence`**: internal-validation view of the aggregate (blueprint's "/admin first" step). **HARD GATE:** consumer display of cross-user aggregates requires `consumerDisplayApproved` (CCO sign-off) — false today, so consumer panels show the honest "still gathering data" state; the own-data panels are fully live. Guard `scripts/intelligence.test.ts` (19 assertions incl. k-anon gate, CCO gate, CROA forbidden-phrase sweep). Metrics BI-OUT-01/02 registered. ⏰ OWNER/CCO: (1) sign off before enabling `consumerDisplayApproved`; (2) counsel review of the outcome-contribution consent copy. Pre-launch: corpus ≈0 observations, so every aggregate honestly reads "insufficient data" until real disputes accumulate — the engine compounds automatically from there.

**Sprint IX — Mail Center (2026-07-13, customer experience on the Sprint VIII architecture):** new `/mail` Mail Center — the source of truth for "did my dispute go out, what happens next?". Built as a **deterministic projection** (`lib/mailCenter.ts`, zero AI, zero network, no MailService/provider/payment/Kai/dispute-gen change) over the REAL source today: the user's `Letter` rows + the §611 clock (`lib/forecast.ts`), rendered in the canonical `MailStatus` vocabulary. Per dispute: recipient/tradeline/bureau/round/class/status/date-sent + **Kai Mail Intelligence** (deterministic §611 window + own-history + one recommendation), a **health card** (6 state-driven states: Waiting normally / Needs attention / Response received / Ready for Round 2 / Escalation available / Completed), and an **expandable timeline** (real stages done/current/pending with timestamps; **provider-mailed stages — payment/printing/carrier/delivery/tracking/certified — are honest "available after live provider integration" placeholders, NEVER faked**). Dashboard stats (generated/mailed/waiting/responses/avg-response/round-distribution real; delivered + mail-spend explicitly reserved). Sidebar gains "Mail Center"; journey page cross-links it (case-timeline merge). Product is **self-mail today** — the UI never claims CreditVector printed/mailed/delivered/tracked anything. Guard `scripts/mailCenter.test.ts` (25 assertions incl. a CROA forbidden-phrase scan + no-provider-stage-ever-done). Sprint X (deferred): live LetterStream HTTP, address validation, payment, certified, tracking, delivery confirmation, provider failover/retry/health — the plumbing behind the now-complete experience.

**Sprint VIII — CreditVector Mail (2026-07-13, architecture; ADR-0011):** new `lib/mail/` module — a permanent, provider-abstract mailing platform. **Provider interface** (`MailProvider.ts`: validate/estimate/create/cancel/track/status/proof/health) with provider vocabulary contained inside each provider; **single-config switch** (`MAIL_PROVIDER` env) via the registry; **LetterStream = Provider #1** in dry-run (real validation/cost/status logic, NO network — live calls gated behind `MAIL_LIVE`, throw `not_wired`), Lob/PostGrid/Click2Mail/PostalMethods as interface-conformant stubs. **Canonical `MailStatus` state machine** (GENERATED→…→DELIVERED→RESPONSE_RECEIVED→CLOSED + CANCELED/FAILED/RETURNED, guarded transitions). **`MailService`** is the only orchestration surface and enforces the two invariants: (1) **Kai never sends** — Kai recommends, a USER approves, `dispatch` refuses unless PAID (only reachable via user APPROVED); (2) **immutable manifest + append-only audit** (identity write-once, enforced in `MailStore.saveProgress`). **Composed pricing** (`MailPricing.ts`: provider cost → platform fee → markup → plan/agency discount → coupon, policy-as-data). **Immutable `MailManifest` + `MailAudit` = the "proof of process"** (self-heal `MailManifest` table, ADR-0001). Each transition emits a fail-open `mail.status` KaiEvent. Guard `scripts/mail.test.ts` (38 assertions incl. the approval-gate + identity-immutability e2e). **NOT this sprint (deferred, per directive):** live API integration, Mail Center/Timeline UI, wiring into the existing "Mark mailed" flow — module is additive + inert (nothing calls it in prod yet), so zero-risk. Rule of Two: all in CreditVector; clean seams are extraction-prep only.

**Sprint VII — Explainable Intelligence (2026-07-13):** **Kai Explainability Layer** — `lib/explain.ts` assembles the STRUCTURED "why" per tradeline (observed · which bureau data contributed · contradictions that raised confidence · which laws apply · what stays uncertain) entirely from real persisted data + the deterministic engines (scoring reasons, cross-bureau conflicts, obsolescence clock, statute/strategy maps) — evidence, never chain-of-thought, uncertainty never hidden, no AI. Rendered by `components/kai/KaiWhy.tsx` inside every DISPUTABLE tradeline row's "Kai's read ▾" expander. Guard `explain.test.ts` (14/14). **Kai Case Memory** — `lib/kaiSeen.ts` (self-heal `KaiSeen` table, ADR-0001) computes "While you were away — since {date}:" on Kai Home from REAL KaiEvents since the user's last visit (structured events only, no freeform AI memory; write throttled to ≥1h to avoid force-dynamic write-amplification; fail-open). **Three required reviews ran (legal + WCAG + adversarial, 14-agent workflow, 7 confirmed — ALL fixed):** (HIGH) the "Dispute →" link was nested inside `<summary>` for every disputable row (WCAG 2.1.1/4.1.2 nested-interactive) → extracted to a sibling overlay; (MEDIUM) **real FCRA bug — §605 clock for collections/charge-offs starts DOFD + 180 days per §1681c(c)(1)**, previously omitted → `reportingOffsetDays()` added centrally to `obsolescence.ts`, threaded through `scoring`/`recommend`/`tradelineInsights` (guards updated); uncertainty text raised to parity with favorable evidence (FTC clear-and-conspicuous); NOT_RECOMMENDED rows now render a coherent "Why I set this aside" panel (no confidence badge, no contradictions); sr-only copies for title-only reasons; Case Memory write throttled + race-hardened. Validated: tsc · build (48/48) · 13 guards green.

**Sprint VI — Intelligence Layer (2026-07-13, blueprint + first slice):** `INTELLIGENCE-LAYER.md` + **ADR-0010 (PROPOSED)** designed the four compounding engines (1 Knowledge/anonymous strategy→outcome patterns = Moat #4 extended; 2 Recommendation/evidence-annotated; 3 Outcome Intelligence/predictive-of-timeline; 4 Institutional Memory/structured product-learning events) mapped to EXISTING substrate — the compounding material is already built/designed (Letter outcome record, BI-FUNNEL-01, KaiEvent, statute engines, KaiAnswer/KaiRecommendation, AiUsage); the sprint mostly *connects* it. Hard gates recorded: cross-user engines blocked on a **consent surface (does not exist)** + k-anonymity (n≥30) + CSO/CCO + (Engine 2) ADR-0006 founder approval. CV-vs-GIOS extraction wall per Rule of Two / ADR-0009: all built IN CreditVector, tagged `extraction-candidate`, ZERO Year-1 extraction. New metrics registered in METRICS.md (BI-OUT-01/02 ❌). **SHIPPED gate-free slice — Engine 3 Tier A:** `lib/forecast.ts` (own-data + FCRA §611 only; predictive of timeline/options, never result; no cross-user data, no new AI, no consent needed) surfaced as a per-letter "What happens next" forecast on the letters page (window + own-history median latency + the 4 enumerated outcomes + contingency path). Guard `scripts/forecast.test.ts` (12/12). **CCO gate ran: GO-WITH-CHANGES applied** — 45-day extension nuance added to the past-window line; own-history "median" `minSample` 2→3 (FTC substantiation). This slice is the seed of the compounding loop: the outcome record it reads is what Engine 1 later aggregates anonymously post-consent.

**Sprint V pass 2 (2026-07-13, the magic moment, `9f53772`):** upload → analysis is now an honestly-narrated staged experience: `/api/reports/upload` streams NDJSON (stage line per REAL pipeline step — extracting/received-encrypting/reading/scoring via a new optional `onStage` hook on `analyzeReportText`; no timers, no fiction), and the client ends on the Kai REVEAL — "I finished reviewing your report" + found/matters-most/first-action/confidence, every number computed server-side from the just-persisted rows via the existing engines (conflicts, §605, high/medium, top target + `recommendStrategy` why + deep-link CTA into the pre-filled letter). Score-impact categories deliberately omitted (CROA). Degrades safely under proxy buffering (all lines arrive at completion = old UX). ⚠️ deployment-skew note: pre-deploy tabs POSTing the old JSON contract get the calm transport error once, self-heals on refresh. **Emotional adversarial review ran (4 lenses × verify, 19 agents, 14 confirmed — ALL fixed, `6cfa21f`):** orphan-report cleanup on pre-analysis failure (retry can't duplicate; post-analysis failure keeps work + says where it lives) · connection-drop copy no longer claims "nothing was lost" (refreshes list, points user there) · reveal top restricted to HIGH/MEDIUM + snapshot-consistent counts · CROA: scoring's "high deletion potential" → validation-duty language (also fixes tradelines rows), pills anchored to dispute GROUNDS not outcomes, encryption-absolute + AI-traceability overstatements corrected · zero-accounts = Kai guidance (not red error), server-fault reassurance ("nothing about your report or your credit caused this"), 401s humanized on upload+re-analyze, §605 explained as the relief beat.

**Sprint V pass 1 (2026-07-13, Apple-phase polish, `a33e74c`):** Kai Home's two progress cards merged into ONE (overall % + per-bureau bars — no two widgets answer the same question); timeline events each carry a rule-based "what it means / what's next" line (outcome-aware for responses; CROA-clean process claims only); landing `.reveal` gained a CSS-only safety net (2.5s forwards animation) so below-fold marketing content can never stay invisible under blocked JS / slow hydration (mobile scroll-race exposed it). Remaining Sprint V candidates: upload analysis-reveal moment (#30), timeline sub-line dedup if it reads heavy, CX-6 cached community summaries (needs ADR-0006 approval + CCO), favicon/OG regen.

**Build Wave 4 (2026-07-12, premium finish; 5-lens × adversarial-verify audit fleet, 40 agents, 30 confirmed findings — ALL applied):** **Dashboard** — every stat changes a decision (Items Resolved replaces Letters Generated; vanity bottom row deleted); Quick Wins deduped against the Kai recommendation (one-recommendation law) and demoted to neutral chrome (success reserved for resolved; brand=recommendation + gold=radar are the only tinted surfaces); two-tier header system (eyebrows, emoji dropped); type scale normalized (11px→xs, three deliberate tiers); rhythm unified (mt-4/p-5); StatCard label-first + tnum + success variant; overnight links got rest-state underline affordance; KAI chip radius unified (dashboard+journey). **Landing/marketing (attorney+investor lens)** — "Every inaccuracy… is flagged" → "Potential…are flagged" (FTC §5); hero mock's REAL creditor names (Capital One/Midland) → fictional (Northline/Crestline; net-impression risk); "Items resolved over time" chart → "Dispute activity" + "On track" → "2 response windows open"; "Nothing slips past a deadline" → verifiable feature copy; "Most popular" → "Recommended"; roadmap pills → tagline cards (funding tagline outcome-implication fixed in `lib/brand.ts`). **Consistency** — onboarding fully rethemed to tokens (was emerald/purple/orange/pink; also killed "start fixing your credit", "what's being fixed", the nonexistent "free trial" offer, and an absolute detection claim — CROA); billing on-token (.card shells, btn-primary CTA, success banner, rose errors, h1 scale) + Kai letters-remaining/reset line from real entitlement fields; identity emeralds → success/brand + Kai first-person discrepancy header; letters raw-blue pills → .pill ocean/brand. **Micro-craft** — `.card:hover` lift scoped to interactive cards only (false-affordance fix; `a.card/button.card/.card-interactive`, reduced-motion updated); `summary` added to the global focus-visible ring; letters: loading state kills the empty-state flash/CLS, 44px touch targets on all row actions; tradelines Dispute link 44px + underline cue + summary focus state. **Kai presence (zero new AI)** — pill hidden on /journey (double-presence); scores single-entry "log the next and I'll hold the trend" hint; settings push claim made capability-accurate. Validated: tsc · build (48/48) · 11 guards green.

## Validation status
`npx tsc --noEmit` clean (2026-07-12) · `next build` ok (2026-07-12) · all 11 guard scripts green 2026-07-12 (see `TESTING.md`).

**CX Review (2026-07-12, docs-only):** `CX-REVIEW.md` — all 42 screens audited (dashboard read in full; form-density measured); verdicts + journey friction fixes; conversational-workflow pattern defined; Agency Command Center + community-Premium cost model + retention additions specced; 12 CX items slotted into `ROADMAP-V2.md` Amendment 1. Notable code finding: dashboard's "Est. Points Recovered" renders literal "~ estimate" (dashboard/page.tsx:177) — replace with Kai Home (CX-2). Decision flagged: CX-12 identity-verification gate (founder/CLO).

**Platform layer (2026-07-12, docs-only):** `platform/` — Gabriel Intelligence blueprint (JARVIS runtime = 7 subsystems generalized from proven CV engines; Rule of Two extraction law — nothing extracted before product #2 commits), Product SDK (11 inheritance packages + 7 enforced inheritances + day-one bootstrap), Founder Intelligence layer (maps live AIOS fleet + adds decision journal/weekly brief/pre-review packs). **G-PLAT-1 RESOLVED (ADR-0009, founder directive):** GIOS does not exist in production; it will be extracted FROM CreditVector; CV runs standalone on Claude Opus 4.8; architecture frozen — customer experience only. Positioning ruled: **AI-powered Financial Reputation Platform**.

**V2 layer (2026-07-12, docs-only):** `PRODUCT-VISION-V2.md` (journey + three-question rule) · `DELIGHT-SYSTEM.md` (D1–D100 + W1–W50) · `FOUNDER-STANDARD.md` (binding gate; Constitution **Article XI** Trust-First added) · `MOAT.md` (12 moats) · `ROADMAP-V2.md` (**the ranked Top-100** — first sprint: #4 AI metering + #5 event engine) · Kai §7 intelligence-layer laws added to `creative/KAI-PRODUCT-DESIGN.md`.

## Next three recommended tasks
1. Unblock the digest: owner sets `COMPANY_POSTAL_ADDRESS`, run test digest (`List-Unsubscribe` already shipped).
2. Run/confirm the encrypt-letters (and encrypt-reports) backfills.
3. Sprint 3 code: deferred perf items (Sidebar context provider, letters/upload server-prefetch) or roadmap #22 loading-truth completion; #16 KaiAnswer store stays blocked on ADR-0006 founder approval.
