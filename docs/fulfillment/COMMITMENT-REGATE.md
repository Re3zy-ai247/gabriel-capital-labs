# Fulfillment Wallet Commitment Model — Adversarial Re-Gate

Single bounded Opus re-gate scoped to the revised wallet commitment model (@ c61f2c8) · read-only · every repository citation independently re-verified.

## VERDICT: NOT READY — foundations sound, one cross-document contradiction restores F4, eight bounded doc fixes

The revised model's foundations need NO redesign: the two-layer boundary, the Wallet anchor lock, the un-floored fold, per-letter settlement grain, the attempt dimension, and the payer-principal model are all correct and correctly argued. What fails is the seam between W1 (wallet) and W2 (recovery/boundary), plus four concrete money paths. Every must-fix is a bounded document correction; none reopens the model.

## Per-defect re-test

| # | Verdict | Core |
|---|---|---|
| F3 overdraft | RESOLVED-WITH-RESIDUALS | Anchor lock real, proof holds under READ COMMITTED. Residuals: cureDeficit reads fold outside the lock (narration only); deadlock proof reasons wallet-only while the txn spans AdminAuditLog + manifest writes with unspecified ordering; no lock timeout → contention surfaces as an untyped 500, not a typed refusal |
| F4 free fulfillment | STILL PRESENT | The original consume-after-void direction is genuinely closed; the SURVIVING path is re-authorization-after-release, which W2 mandates as "same attempt reused" while W1 mints attempt+1 — no value of attempt satisfies both, so the wallet no-ops the re-authorization and the certified letter mails for zero net wallet effect. See N1 |
| F5 partial settlement | RESOLVED-WITH-RESIDUAL | All-or-nothing hold creation is genuine Postgres atomicity; 2-of-3 = 2 settle + 1 release. Residual: AuthorizationGroupView.groupStatus undefined for a letter with two attempts under a forwarded group id → "2 of 3" misreports after retry |
| F6 funding | RESOLVED-WITH-RESIDUALS | amount_total + promo-off + clawback correct; in-flight-holds deficit folds correctly all three ways. Residuals N4, N7 |
| F7 payer model | RESOLVED-WITH-RESIDUALS | Mechanism verified against lib/session.ts (currentAccount/impersonationContext/currentWorkspace exist as cited); impersonation-block reasoning correct. Residuals: N6 breaks the schema fix; the approve route needs two identities (payer + subject) and following "never currentUser()" literally deletes the only shipped cross-client ownership check; which-wallet-pays answered by prohibition not resolution; principalType stamped-once never re-derived; agency-staff granularity disclosed |
| F9 retry/claims/quarantine | RESOLVED-WITH-RESIDUALS | Provider outcomes ARE attempt-bound (mail_<letterId>_a<n>); CANCEL_REQUESTED cannot race settle. Residuals: attention flag gates nothing in the money layer (convention not mechanism); wallet claim-key grammar in three incompatible forms; attention/cancelRequest storage blocked by canon (N5) |

### Surviving F4 path
authorizeGroup(mail_X, attempt 1) → hold −P → sweep release(attempt 1) → +P, stage PAYMENT_VOID → operator re-authorizes: RECOVERY §4 row 12 + BOUNDARY §5/§7 MANDATE "same attempt reused" → W1 §5.2 replay branch finds attempt-1's authorize row, returns ok:true with NO new debit and never checks for a settle/release sibling → caller believes a hold exists → Submit → provider accepts → settleHold(mail_X,1) hits the released check → already_released → certified letter mailed, net wallet effect zero. At attempt:2 the wallet is correct but W2 forbids a new manifest, so DisputePackageLetter.attempt (declared single owner) stays 1 and settle derives attempt 1 and fails identically. No attempt value satisfies both documents.

## New findings

| # | Finding | Sev |
|---|---|---|
| N1 | F4 restored via scenario 12; same replay branch also sums requiredCents over ALL holds vs a fold already containing them → false insufficient_funds on genuine replays whenever the wallet held < 2× group cost | CRITICAL |
| N2 | PROVIDER_ACCEPTED is in the shipped CANCELABLE set (MailStatus.ts:43) → canTransition(PROVIDER_ACCEPTED,CANCELED)=true and MailService.cancel can drive it AT the settlement boundary; W2 models post-acceptance cancel as off-machine only and omits the edge → manifest CANCELED + wallet settled, no ledger event, no rule | HIGH |
| N3 | Sweep release signal is ledger-only ("authorize with no sibling older than staleAfter") with no manifest-state filter → also matches SUBMITTED-in-flight; TOCTOU against an acceptance landing between poll and release; NO matrix row for "acceptance arrives after the hold was released" | HIGH |
| N4 | Clawback uses charge.amount_refunded (cumulative) while charge.refunded fires per-refund with distinct event.id keyed chargeback:<eventId> → two partials over-count; no dispute.closed(won) handler → won dispute leaves debit permanent; deficit blocks agency-wide amount-independently → one bad dispute freezes the whole agency book. Not in register | HIGH |
| N5 | attention/cancelRequest stored via ALTER TABLE MailManifest ADD COLUMN IF NOT EXISTS; MailManifest is a self-heal raw-SQL table; CLAUDE.md gotcha #1 (owner-ratified) forbids new runtime-created schema; schema-safety guard fails on self-heal DDL outside the frozen allowlist → F9-i "ELIMINATED" rests on a mechanism blocked by the same canon as F1 | MED-HIGH |
| N6 | WalletLedger.actorId is non-null Restrict FK to User but fund/clawback (webhook) and sweep settle/release are system-driven with no valid actor → insert fails first use (§8.3 passes literal "system"). AdminAuditLog.actorId is deliberately a plain String, no FK. Also only authorize carries onBehalfOfId → settle/release/clawback drop the on-behalf-of attribution | MED-HIGH |
| N7 | Funding keyed to event not payment: no payment_status==="paid" check (the string appears nowhere in repo → delayed-notification methods complete before funds exist); no currency assertion (Adaptive Pricing is an account toggle, no code change); subject topup:<eventId> doesn't dedupe two events for one payment | MED-HIGH |
| N8 | FINAL REVIEW: (a) W3 argues it must PRECEDE wallet authorization; W1 §11/§15 place it AFTER authorizeGroup — direct conflict on Founder ruling #3's moment, not docketed; (b) record is four client booleans checked only for "four trues", no server binding/nonce/expiry → forgeable and replayable while presented as compliance evidence | MED |

## Register-honesty verdict: honest in posture, inaccurate in three claims
Carried correctly: F1 STANDS; F2 STANDS as hard counsel precondition (S7 note verbatim in all headers); F9-ii/iii honestly contingent; F10/F12 REDUCED not eliminated; anchor-lock cost, agency-staff residual, and scenario 9/16 FOUNDER-GATE forks all disclosed; Gate D + CROA not soft-pedaled; PII-denylist compliance verified.
Inaccurate: (1) F4 "ELIMINATED" is wrong — should be STILL PRESENT until N1 lands; the merge printed both halves of the contradiction one table apart. (2) F9-iii's "same key in BOTH domains" is false — W1 uses wallet:<subjectId>:<attempt>:<entryKind>, W2 uses wallet:<subjectId>:<transition>:<attempt>; three grammars, none reconciled; Claim.key is the global PK so a writer/reader disagreement silently disables the outer dedup (money-safety survives on the inner lock; defense-in-depth does not). (3) Join ruling 1 "rename-only, no content conflict" is false — W2 names ~19 kaiCopyClass handles, W3 (written blind) wrote 10; ≥6 have no copy, and W3's CANCELLATION_CONFIRMED "your hold was released in full — nothing was charged" is FLATLY FALSE if re-keyed onto W2's CANCEL_CONFIRMED_RARE where the wallet STAYS settled — the hardest disclosure in the program currently has copy saying the opposite.
Incomplete: W3's money narration is entirely self-pay voice with no on-behalf-of variant (wrong for managed clients whose agency paid, contra W1 §9); register omits the agency-wide deficit blast radius (N4), the runtime-DDL dependency (N5), the FINAL REVIEW ordering conflict (N8a), the transaction-timeout path (F3-c); RecoveryVerdict.basis is an open string (Vendor Opacity DTO unenforced until it is a closed union); FOUNDER-SUMMARY + HTML still carry the prior gate's verdict.

## Must-fix (bounded; none reopens the model)
A — money safety (blocking): (1) one re-authorization rule in one document — either PAYMENT_VOID re-auth mints attempt+1 (amend RECOVERY §4 row 12 + BOUNDARY §5/§7) or W1 adds a distinct reauthorize op against a terminal attempt; whichever also moves DisputePackageLetter.attempt. (2) W1 §5.2 replay branch: refuse attempt_already_resolved when the authorize row has a settle/release sibling; compute requiredCents over only non-existing holds. (3) sweep releases gated on manifest state + a definitive provider negative; reconcile scenario 4 vs 12; add the missing "acceptance after release" row. (4) clawback per-refund amount keyed on refund/dispute id + dispute.closed(won) compensating entry; scope or disclose the agency-wide deficit blast radius. (5) rule on PROVIDER_ACCEPTED ∈ CANCELABLE — remove (product change) or define the wallet consequence; restore the edge to W2 §5/§6; fix W1 §7.3 pt 4.
B — correctness: (6) WalletLedger.actorId nullable + actorKind (or the AdminAuditLog no-FK precedent); propagate onBehalfOfId onto settle/release/clawback. (7) assert payment_status==="paid" + currency==="usd"; key fund on the PaymentIntent; state async_payment_failed/expired behavior. (8) re-plan attention+cancelRequest storage against gotcha #1 (a named decision, not ALTER TABLE). (9) rule the FINAL REVIEW ordering once; bind confirmation to a server-issued single-use expiring token carrying contentHash/warningVersion/totalCentsShown/policyVersion.
C — register: (10) F4 → STILL PRESENT; unify the claim-key grammar in one place; withdraw the "rename-only" join ruling and commission the ~6 missing copy classes (esp. CANCEL_CONFIRMED_RARE); add on-behalf-of voice to every money line; close basis to a union + add to the DTO guard; add the four omitted disclosures; update FOUNDER-SUMMARY.

Set F3/F5/F6/F7 to RESOLVED-WITH-RESIDUALS (not ELIMINATED). With A+B+C applied this is a READY-WITH-DISCLOSURES candidate on the next pass. CROA §404, §611 framing, and Gate D Phase −1 remain hard preconditions regardless — all three carried correctly today.
