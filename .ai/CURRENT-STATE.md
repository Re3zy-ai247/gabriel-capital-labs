# Current State

**Operator Network write-path hardening + search (2026-07-19, `feat/operator-network-shell`, PUSHED `51d28a2`):** three live gaps closed additively, no new records/models. (1) **CROA screening on member posts** — thread + reply creation had NO compliance screen (the Brief's reader comments have had one since launch); `screenCommunityText` (lib/community.ts) now runs BEFORE any write and REJECTS with 422, never rewording — the platform neither hosts the claim nor puts words in a member's mouth. The letter engine deliberately keeps substitute-and-show (the user reviews/approves final text, so the rewrite is visible, not silent). (2) **Write throttles** — thread/reply creation were unlimited; now 20/hr and 60/hr per account, applied before the write. (3) **Search** — zero-hydration GET form, DB-side query (filtering the capped 200 would silently omit older matches), results scope queue+strip together per the guard-pinned strip↔feed invariant while ambient/doors/Network State keep describing the whole network; empty-result and end-cap copy answer the search instead of claiming the room is empty. New guard `scripts/community-screen.test.ts` 30/0. Suite 39/39 · operator-shell 94/0 · tsc 0 · build ✓ (`/community` 6.19 kB, unchanged) · preview Ready. ⚠️ Authed visual still needs owner click-through (local has no DATABASE_URL; preview URLs 401 to anon by Deployment Protection).

**⚠️ COMPLIANCE FIX — §609 rule was half-dead in production (2026-07-19, `8c5d6f7`):** `lib/compliance.ts`'s §609 deletion-myth pattern opened with `\b` before `§`. `§` is a non-word character, so that boundary can never match — the rule only ever caught "section 609"/"fcra 609"; the literal **"§609" / "§ 609" spellings passed the scrubber untouched on every generated letter**. Fixed (boundaries now on the word-initial branches only); verified all six myth spellings caught and lawful §609/§611 uses untouched (no over-scrub). The CROA bar names §609 deletion myths explicitly.

**🔴 Vercel plan VERIFIED = Hobby (2026-07-19, `api.vercel.com/v2/teams` → `billing.plan: hobby`).** This is the hard blocker the Living Intelligence package named: real-time chat/presence/typing (SYNC polling) is **not shippable on Hobby** — ~7-9M function invocations/mo at 100 concurrent users, and cron granularity caps at daily/2 max (both already used by Brief). Skew Protection is separately blocked on Hobby (`invalid_billing_plan`). **Owner decision: upgrade to Pro before any realtime/presence work is scheduled.**

**Mission Control — the Watch Floor (2026-07-19, `feat/operator-network-shell`):** first production surface restyled under GXL via the full Claude×Higgsfield loop (forensics → 5 rendered concept directions on the real layout → adversarial review → synthesis). Class-level restyle of MissionControl + ExecutiveQueue + dashboard wrapper: record/engraved registers, slab key-pool luminance, folio hairlines replacing card-per-row, status-derived planes (actionable=lifted+gold flag · settled past=recessed), chips retired in the queue, verdict-column triptych, engraved colophon, GxlField on real queue aggregates, GxlPull on the next-action receipt + every queue entry. Fixed two standing law violations (entrance animations; green "On track" pill). IA/workflows/semantics untouched. tsc 0 · lint ✓ · guard 94/0 · build ✓ (3.13 kB). ⚠️ Authed visual = founder preview click-through (local 500 = pre-existing no-DATABASE_URL, A/B-proven). Below-fold views (MissionQueue/Roadmap/Knowledge/Builder/CommandCenter/Readiness) deliberately deferred.

**Operator Network Phase 1.2 — Intelligence Operations Center (2026-07-19, `feat/operator-network-shell`):** interaction-language redesign on the frozen Phase-1 architecture. Mental model: WORKSPACES (Executive Briefing · Operations Floor · Success Intelligence · Strategy Council · Operations Lab · Kai Intelligence — display labels over FROZEN category keys; doors carry live derived state), a SECTIONED attention queue (Requires attention / Active intelligence — exact partition), Situation-report panel, per-room server-chosen ambient atmosphere, and the Living Intelligence layer (ONE delegated-listener island: cursor computation-reveal via CSS vars on .ops-card; room-hover breath-bias on the ambient field, eased+decaying; 6s border breathing ONLY on zero-response unlocked briefs; all silent under reduced-motion/Save-Data). Full forum-vocabulary sweep (briefs/responses/workspaces, detail page included; house-rule compliance sentence intact). No records/routes/contracts changed. Validation: guard **94/0** (incl. bite-tested regexes + partition proof) · tsc 0 · build ✓ (`/community` 6.19 kB) · lint ✓ · gate verified 1440/1024/390, zero console errors. ⚠️ Multi-agent adversarial review was capacity-blocked (session limit); an inline 5-lens review ran instead — re-run the fleet post-reset if desired. Design risks recorded: a room of all-open-loops breathes as a group; pinned items sit under "Requires attention" per the founder comparator.

**Operator Network Phase 1.1 — Intelligence Operations Center (2026-07-19, branch `feat/operator-network-shell`, PUSH PENDING/UNMERGED):** founder-frozen decisions implemented: (1) **customer-facing name = "Operator Network"** everywhere (page chrome incl. detail page, sidebar, pricing matrix row `["Operator Network","n","y",…]` + Professional bullet, landing section + SiteNav, module registry, moderation email; route stays `/community`); (2) **access = ALL paying members** — `canAccessCommunity` now delegates to the canonical `isPremium` (fail-closed; past_due keeps access per Stripe grace-window design, documented; managed clients excluded; matrix grants COMMUNITY to professional + community perms moved into PAID_PERMS); (3) **attention queue** — deterministic comparator pinned → open-loops (0 responses, unlocked) → recency → id (pure, in `format.ts`, fixture-tested); (4) masthead → **operational strip** (h2 + channel-scoped derived counts); (5) card recomposed state-first (attribution demoted, "responses" vocabulary, "In discussion"); (6) Now panel → **Network State** (Needs attention · Recently active · Kai activity · Pinned; contributors widget removed); (7) rail = working destinations only (SOON items + green HERE removed); (8) **ambient canvas data-driven** (density/breath/accents from real aggregates) + HIGH perf fix: viewport-bounded sticky canvas (O(viewport), not O(feed)); (9) light-mode gold contrast rule; a11y fixes (heading outline, role=status, aria-label removal, slate-400, fixed mobile composer trigger). **34-agent adversarial workflow: 29 confirmed findings, ALL fixed + guard-pinned.** Validation: tsc 0 · operator-shell **75/0** · capability-matrix 40/0 · platform-foundation 79/0 · build ✓ (5.72 kB route JS) · lint ✓ · gate verified desktop/tablet/mobile, zero console errors. Authed shell visual = owner click-through on the Vercel preview.

**Operator Network Phase 1 — shell (2026-07-19, branch `feat/operator-network-shell`):** the Community Hub page rebuilt as the server-rendered Operator Network shell per the frozen Architecture Blueprint + Design Bible v1.0. UI-ONLY: same agency-only gate (copy verbatim), same CommunityThread/Reply records, channels = existing `category` values, composer behavior preserved (multipart + askKai + attachments, extracted to a client island). New: 3-pane layout (rail · feed · Now panel), deterministic ambient canvas (reduced-motion/Save-Data/visibility-aware, deferred post-LCP, no libs), shape-matched loading skeletons, honest end-cap ("the feed ends"), mobile chips + `<details>` Now sheet + sticky composer trigger. NO new records/routes/flags/entitlements (guard-proven). Validation: tsc 0 · `operator-shell.test.ts` **33/0** · build ✓ (`/community` 5.53 kB route JS) · lint ✓ · gate screenshot verified (desktop+mobile, zero console errors). Authed 3-pane visual = owner click-through or Vercel preview after push. Detail page `[id]` deliberately untouched (Phase 1.1 candidate).

> Authoritative operational snapshot of the **CreditVector app**. Compact by mandate — never a
> chronological log. **GIOS/kernel current truth → [../architecture/FOUNDER-CONTEXT.md](../architecture/FOUNDER-CONTEXT.md)** (do not restate it here). **Full sprint history (V–XXV, Build Waves)
> → `ARCHIVE/CURRENT-STATE-full-2026-07-15.md` + ADRs 0010–0027 + `KAI-ENGINEERING-JOURNAL.md` + git.**

**Last updated:** 2026-07-17 · **Branch:** `main` — the four stacked sprints below are now
**COMMITTED (`ed8cbf2` → this commit) and UNPUSHED**; prod still runs `56e2ed4`. · **Prod:** LIVE at
https://www.creditvector.app (Vercel auto-deploys on push to `main`) · **MAIL_LIVE OFF**.

⚠️ **Commit order note (2026-07-17):** Phase C landed BEFORE Phase B, inverting the planned
B-then-C sequence. `lib/platform/health.ts` (Phase B) imports `kaiSelfTest` + `caseMemoryStoreEnabled`
from `lib/intelligence/` (Phase C), so B-first would not typecheck. The dependency is real and
one-directional — Phase C imports nothing from Phase B. The Kai Experience language audit is its own
commit (`b7a34e1`) EXCEPT its pricing-matrix subset, which is physically inseparable from the matrix
regrouping and landed in `ed8cbf2` (same-hunk delete/re-add + atomic rewrites; splitting would have
required fabricating an unvalidated intermediate matrix — precedent `ad239e3`). No Stripe commit
exists: that migration already landed in `ad239e3`; nothing Stripe-related was dirty.

**Kai Experience Sprint — language audit (2026-07-16, COMMITTED `b7a34e1` — push pending):** Phase-1 branding-hierarchy pass (Platform=CreditVector · Employee=Kai · features=plain English · GIOS invisible). Removed unnecessary "AI" from user-facing FEATURE labels + made Kai the consistent actor: site title/OG "AI-Powered Credit Intelligence Platform"→"The Credit Intelligence Operating System" (layout.tsx + `lib/brand.ts` tagline — the site-wide rendered source, caught as an orphan by review); pricing matrix "AI report analysis"→"Credit report analysis", "AI letter refinement"→"Letter refinement"; pricing eyebrow + page `<title>` de-AI'd; landing how-it-works "The AI reads/refine with AI"→"Kai reads/refine with Kai" (+ module tagline + Showcase "Drafted by Kai"); billing/onboarding/agency/identity feature labels; AiPlan card "AI Action Plan"→"Action Plan" (badge already "KAI Intelligence") + its 402 string. **DELIBERATELY LEFT (non-negotiables):** the compliance disclaimer + privacy disclosure ("AI-powered educational tool" — legal wording, transparency), `lib/stripe.ts` product description (billing — deferred to a coordinated Stripe-metadata pass), code comments. **Rest of the sprint (Phases 2-10: activity feed, Mission Control, executive summary, decision explanations, confidence, empty states, delight) NOT rebuilt — already implemented by KaiEvents · MissionControl/ExecutiveQueue · CVI/`explain.ts` · Phase-C confidence engine · the 16-state Kai Catalog + Character Bible; parallel builds would duplicate existing systems.** 2-lens adversarial review → 3 orphan HIGHs (brand.ts tagline, pricing title, landing AI-actor) all fixed; matrix-truth findings were misattributed CX-sprint work (git diff shows all stacked uncommitted sprints). Guards 35/35, tsc 0, build ✓. Zero entitlement/pricing/billing/platform logic touched — display strings only.

**GIOS Phase C — Kai Intelligence Architecture (2026-07-16, COMMITTED `677c116` — push pending):** Kai's deterministic intelligence layer, built INSIDE the CVI charter (`lib/intelligence/` — inventory-first: explain/recommendationIntel/forecast/outcomeLedger/missionEngine/roadmap already owned their domains; Phase C adds only the UNIFYING layer). New CVI modules: **graph.ts** (Unified Credit Graph — typed nodes/edges over the whole case, citable `node:` refs, recipientType-aware dispute windows) · **reasoning.ts** (9-stage pipeline Facts→…→Expected-outcome; NO LLM/clock/randomness; no-hallucination validator with teeth incl. focusRef + forbidden-language over EVERY text surface; LAW LAW: citations built from `lib/statutes.ts` verbatim, §611/§623/FDCPA-§809 branched by recipient; STRATEGY LAW: selection stays with canonical `lib/recommend.ts` via injected `StrategyHook` — reasoning names no strategy itself; per-item confidence, never "high" without verified outcomes) · **caseMemory.ts** (projection-first — the case record IS the memory; `KAI_CASE_MEMORY` store gate registered + architecture-checked OFF per ADR-0006) · **planner.ts** (goal-directed plans; FAIL-CLOSED on validation errors — `degraded` plans withhold steps; CROA-safe display labels) · **proactive.ts** (receipt-carrying signals; `validateSignals` CROA net; per-kind CAN-SPAM classHint with counsel_review routing; notify bridge decision-only, topic-label summaries, ADR-0027 intact) · **portfolio.ts** (agency intelligence: documented 0-100 risk formula, deterministic ranking) · **selfTest.ts** (the ONE synthetic fixture; feeds the dashboard's Kai panel, labeled synthetic). Dashboard: Kai Intelligence section on `/admin/platform` (self-test PASS, explainability %, letter outcomes from DB). **4-lens adversarial review: 30 findings — BLOCKER (strategy-engine contradiction) + 2 legal HIGHs (proactive outcome-promise wording; §611 misattribution on furnisher/collector windows) + 11 more fixed pre-report; deferred with notes: production loader (graphInputFromSnapshot — next sprint, nothing user-facing consumes Phase C yet), proactive↔opportunities + confidence-ladder convergence, planner↔missionEngine boundary carve, real notify adapter.** Guards: **kai-reasoning 50/50** (new), suite **35/35**, tsc 0, build ✓. Nothing user-facing changed; ADR-0005/0006/0027 all honored.

**Platform Phase B ACTIVATION SPRINT (2026-07-16, COMMITTED `48baf85` — push pending):** the dormant capability platform is now BUILT and flag-gated (nothing live changed). (1) **Capability Engine:** B2 pure resolver (`lib/os/host/tierResolver.ts`, injected matrix, fail-closed→explorer) + `lib/os/kernel/quota.ts` (absent key=0 fail-closed) + `lib/os/host/billingTier.ts` (PlanTierProvider port; DB plan→tier; capabilities drive plans, never reverse) + **B3 adapter in `lib/entitlements.ts` behind `CAPABILITY_PLATFORM` (default OFF)** — byte-identity with legacy gates guard-pinned over realistic user shapes; B4 route flips remain owner-gated. (2) **Team/Workspace foundation** `lib/os/platform/teams.ts` (owner/operator roles→PEP permission sets, invitation state machine, self-heal tables) behind `TEAM_FOUNDATION` (OFF, fail-closed). (3) **Session foundation** `lib/os/platform/sessions.ts` (device registry, session inventory, revocation tombstones, risk events, 2FA hook, pure admission evaluator that ALWAYS returns `enforced:false`) behind `SESSION_FOUNDATION` (OFF) — enforcement is CSAP-1, separately gated. (4) **Module registry** `lib/platform/modules.ts` (8 modules on the spine incl. counsel-gated Funding Hub/Business Credit OS as zero-claim shells) + `/modules/[slug]` (404 unless flag on AND ADMIN). (5) **Analytics layer** `lib/analytics/aggregate.ts` (pure funnel/adoption read models over ProductEvent; `instrumented:false` honesty) + `/api/admin/analytics`; 4 new server-only events. (6) **Executive Platform Dashboard `/admin/platform`** (+AdminTabs) — every card labeled measured vs assessment; architecture checks computed from real booleans. Guards: **platform-foundation 79/79** (new), suite **34/34**, tsc 0, build ✓. MAIL_LIVE + KERNEL_DURABLE remain OFF; no PEP bypass; no pricing↔authorization coupling; only user-facing live-route change = fail-open `workspace_created` analytics emit in agency clients POST. **6-lens adversarial review: 38 findings; all 7 HIGHs fixed pre-report** — one-resolution-law fix (matrix now flags the full capability universe per tier, matching kernel resolve() fail-closed flag semantics; guard-pinned through real resolve()), byte-identity gap closed (isAgency FLOOR in planTierFromUser for downgraded-agency shapes; new golden shapes + bare-shape divergence pins), ownership boundary restored (role bundles → `config/rolePermissions.ts`; persistence → product-side `lib/platform/{team,session}Store.ts`; GIOS zone is now pure mechanism), unknown-role → EMPTY permissions, hardcoded-true architecture check made falsifiable, Funding Hub P+ bullet re-qualified "(not a lending decision)", analytics failure≠zero-state (available flag) + deterministic ordering, canonical ACTIVE_SUBSCRIPTION_STATES. **Accepted debt (recorded):** tier vocabulary not yet genericized for multi-app GIOS (defer to the multi-app milestone); `/api/admin/analytics` standalone route reserved (dashboard embeds analytics today); campaign.compose/notify.plan host↔matrix divergence stays pinned in guards for conscious B3/B4 resolution. ⚠️ Deploy note: GitHub→Vercel auto-deploy did NOT fire on the 2026-07-16 push (manual `npx vercel --prod` used, verified live) — check the integration before relying on push-to-deploy.

**Conversion & Product Experience Sprint (2026-07-16, COMMITTED `ed8cbf2` — push pending):** pricing experience elevated to outcome language (workflow/business outcomes only — never credit-result outcomes) across all 7 tier cards; comparison matrix grouped under 5 scannable section headers (truth values unchanged; API+Webhooks rows merged — were identical; Funding Hub/Business Credit OS Professional cells corrected s→n to match Professional+ exclusivity); thesis band de-anchored from ChatGPT; TrustBar added to /pricing + "No contracts · Cancel anytime · never a credit pull" line under consumer cards; +1 FAQ (outgrow-your-plan, factually verified vs creation-gating code); Enterprise reframed as partnership. **8-lens adversarial review (6 personas + CCO + consistency): 59 findings, all 5 cross-confirmed HIGHs fixed pre-commit** (falsifiable absolutes "deadlines never slip"/"never miss" removed from LIVE tiers; quantified claim for unbuilt bulk-actions removed; matrix contradiction fixed; mobile sticky group-header bug fixed). Deliberately NOT changed without owner: Agency blurb "Built for solo operators" (owner-approved positioning; review suggests "solo operators and small shops" — see report). Future UX design package (session insertion points, team-management UX, module-nativeness audit) → `~/Documents/Gabriel-Capital-Labs-AIOS/FUTURE-UX-DESIGN-PACKAGE.md`. Validation: tsc 0 · build ✓ · guards 33/33.

**Stripe Professional Migration (2026-07-16, owner-approved):** removed the dead `STRIPE_PRICE_ID` override branch (verified unset in ALL Vercel envs → lookup-key resolution is the sole production path); renamed the code catalog product `premium` → "CreditVector — Professional"; added product-name self-heal to `reconcileTaxCodes`; swept residual user/admin-facing "Premium" → "Professional" (paywall, admin MRR tooltip, admin sync message). Live Stripe changes (rename catalog product, Option-A consolidation of the split-brain $99 monthly onto the Professional product via `transfer_lookup_key`, archive the orphan `prod_UhM6…`, canonical product metadata) executed via direct API post-deploy. ⚠️ Debt: stale $799/$7,990 Agency Pro prices remain (agency_pro is coming-soon + removed from checkout whitelist → unreachable); the admin "Sync products to Stripe" route would create the $699 `_v2` prices — **do not run it until Agency Pro is activated for sale.**

**Platform Phase B — B1 COMPLETE (2026-07-16, dormant):** capability engine foundation per the approved architecture package (`~/Documents/Gabriel-Capital-Labs-AIOS/PLATFORM-PHASE-B-ARCHITECTURE.md`). `lib/os/kernel/tiers.ts` (GIOS mechanism types) + `config/capabilityMatrix.ts` (injected CV data, 7 tiers × 16 capabilities) + `scripts/capability-matrix.test.ts` (**38/38**: lockstep law vs `agencyClientLimit`, grandfather clause, two-branch superset laws, gated-capability flags OFF, session limits declared-not-enforced, live-equivalence on the paid axis, and the KNOWN DIVERGENCE pinned exactly — host grants `campaign.compose`+`notify.plan` free vs matrix pricing them, to be resolved consciously at B3). Zero production consumers; no route behavior changed; KERNEL_DURABLE off. **B1 STOP gate reached — next is B2 (pure resolver + quota + billing port, dormant), owner-gated.**

**Uncommitted, working tree (2026-07-16) — Agency Pricing & Packaging update (owner-approved, stacked on the Polish Sprint):** Agency $399/**15** workspaces (solo operator) · Agency Pro **$699**/40 (growing team) · Scale $1,299/**100** (established) · Enterprise custom. Live cap source `agencyClientLimit` → 15/40/100/null (creation-gating only; existing clients never locked); tier-aware 402 + roster shows honest `N / limit` + 80% capacity banner (`/api/agency/clients` GET now returns `limit`). Stripe `AGENCY_PRO_PRICE_CENTS` 79900→**69900** (+ yearly 699000) with legacy 79900/799000 still resolving to agency_pro in `planForPrice`; product descriptions updated (⚠️ owner must reconcile any already-provisioned $799 Stripe prices — archive + re-provision via admin Billing sync). Kai's AGENCY TIER FACTS updated (was speaking $799/20-client). **Dormant Phase-B B1 files added (zero consumers): `lib/os/kernel/tiers.ts` (GIOS types) + `config/capabilityMatrix.ts` (CV data — all 7 tiers, 16 capabilities, limits incl. CONCURRENT_SESSION_LIMIT 1/3/5/10-default-configurable; lockstep law with agencyClientLimit documented).** Concurrent sessions NOT enforceable (stateless JWT, no registry) → not advertised anywhere in UI; implementation-grade session-architecture package (CSAP-1) at `~/Documents/Gabriel-Capital-Labs-AIOS/CONCURRENT-SESSION-ARCHITECTURE.md`. **10-lens adversarial review (33 agents) → 24 confirmed findings = 6 root defects, ALL FIXED:** (1) agency_pro removed from checkout whitelist (was API-purchasable at stale $799 while marked "Coming soon"); (2) agency_pro lookup keys versioned `_v2` (Stripe prices amount-immutable; old keys would forever resolve $799 — `planForPrice` prefix-match keeps legacy subs mapping); (3) **grandfather clause** in `agencyClientLimit` (pre-2026-07-17 accounts keep sold caps: Agency 20, Agency Pro unlimited — prices AND limits now both grandfathered); (4) 402 + capacity-banner copy honest ("coming soon", never buy-now for unpurchasable tiers) + plan-aware next-tier (agency→Pro, pro→Scale, scale→Enterprise) + truthful over-cap phrasing; (5) matrix superset fix (MEMORY added to agency_pro/scale/enterprise per pricing page) + two-branch superset law documented; (6) billing $699 display OK once (1) closes the purchase path — owner verifies zero agency_pro subs exist in Stripe. Re-validated: tsc/build(53)/guards green.

**Uncommitted, working tree (2026-07-16) — Product Polish Sprint (pre-Private-Alpha):** 25 files changed + 3 new; typecheck/build(53 pages)/guard-suite all green. (P1) 8 launch-blocking copy/compliance fixes: removed fabricated "+N pts est. impact" score projection (strategist page + `/api/strategist` route) → "Dispute strength" label; removed upload "accuracy guarantee"; softened mail-queue "locks in this price" + journey "Price locked in" (Stripe/UDAAP); inverted letter CTAs (Print primary, "Mail via CreditVector (soon)" demoted — MAIL_LIVE off); unified plan naming Premium→**Professional** across billing/help/onboarding/identity/letters/landing (internal `plan:'premium'` + Stripe product names UNTOUCHED — Stripe product still "CreditVector — Premium", owner rename pending); aligned Agency Pro price $699→$799 (matches `lib/stripe.ts` money-system-of-record); help-page dead search/buttons removed, SLA softened, §611 process language. (P2) KAI persona unified as **Credit Intelligence Officer** (calm, evidence-driven analyst, never chatbot/promotional) in `KAI_SYSTEM` + strategist-plan prompt + KAI const; "AI Strategist" nav→"Strategy Desk"; SECURITY/COMPLIANCE prompt blocks intact. (P3) Strategy plan now persists **client-side (localStorage, per-user key)** — survives refresh/nav + staleness nudge; server-side durable store deliberately NOT built (ADR-0006 gate on persisting Kai AI outputs — same gate as blocked `#16 KaiAnswer store`); documented for owner approval. (P4) Minimal `ProductEvent` analytics sink (`lib/events.ts`, self-heal ADR-0001, fail-open, no PII) + `POST /api/events` (auth + whitelist, no spoofing) — 10 alpha funnel events wired. (P5) print-page "How to mail this" screen-only guide. **Adversarial 5-lens review of the diff → 2 CONFIRMED findings FIXED** (server-side 402/checkout error strings still said "Premium" and rendered beside the renamed "Professional" CTAs at paywalls — now consistent in `strategist/plan`, `letters/generate`, `identity/letter`, `stripe/checkout`) + 2 consistency/accuracy fixes (`communityShared` "master agent"→Kai blurb; `dispute_completed` now fires only on the actual transition into RESOLVED); all other findings refuted (notably: dead `estimatedPointImpact` in `lib/scoring.ts` is now zero-caller — recommend deletion as follow-up; localStorage plan not cleared on logout — per-user-keyed so no cross-user display, minor residual only). **NOT committed/pushed — awaiting owner review.**

**Committed locally, push pending (2026-07-16):** KAI-first branding pass — all user-facing foundation-model references removed (AiPlan card → "KAI Intelligence" badge + new description; billing bullet; identity/strategist unconfigured-AI notices; SETUP.md "refined by" line) + KAI identity policy in `KAI_SYSTEM` (never expose provider/model/vendor/routing; identify only as KAI) + strategist-plan persona branded as KAI. Guards green (kai-sanitize 8/8, kai-manifest 44/44, tsc). Internal model config/comments/engineering docs untouched. Owner OK required before push→deploy.

## What's live now
- **CreditVector app:** all Sprints XII–XXV + Pricing V2 shipped to prod (7 pricing tiers; LIVE
  checkout only for Explorer/Professional/Agency; rest "Coming soon"). History → ADRs/archive.
- **GIOS kernel layer (active work):** CreditVector is being wrapped as **Plugin #1**. Kernel in
  `lib/os/`; 6 capabilities registered (5 credit + platform `notify.plan`), all byte-identical,
  **no live route flipped**. Current sprint + roadmap + risks → **FOUNDER-CONTEXT.md**.

## Production integrations (all live)
- **Stripe** LIVE (`sk_live`), webhook verified, catalog synced, letter-pack credits idempotent.
- **Resend** email live from verified `creditvector.app` (deliverability warming).
- **Anthropic** live (Opus 4.8 via `LLM_MODEL`); daily Brief-ingest + weekly digest crons
  (`vercel.json`), `CRON_SECRET` set.

## Working major modules (all shipped + verified)
Report upload → AI parse/analyze → tradelines/scoring → strategist → dispute letters (+ round 2,
response analysis) · Community Hub + Kai · Support tickets · Encrypted attachments & identity docs ·
Agency workspaces · Billing/entitlements · **Brief** (news feed, Phases 1–3: publish flow,
likes/bookmarks/shares, post-moderated comments, RSS+PDF automation draft-only, YouTube embeds,
weekly digest built) · Admin dashboards (Overview+churn, Product Health, Marketing, Automation) ·
Web Push · Password reset · Mail Center + Campaign engine (self-mail today; `lib/mail/` provider
pipeline dry-run, MAIL_LIVE off).

## ⏰ Pending OWNER actions (raise at session start — PRESERVED)
1. **`COMPANY_POSTAL_ADDRESS` env var** — weekly Brief digest is built & live (`e699eb6`) but sends
   NOTHING until this is set in Vercel prod (CAN-SPAM footer). Then: owner subscribes in Settings →
   "Send test digest" from `/admin/brief` → verify.
2. **One-time backfills** — admin console: `fetch('/api/admin/encrypt-letters',{method:'POST'})`
   (encrypts pre-existing plaintext Letter rows; idempotent). Also `/api/admin/encrypt-reports` if
   never run. Status: NEEDS CONFIRMATION whether either has run.
3. **Counsel sign-offs (Article IV):** (a) CROA "educational, not credit-repair" positioning;
   (b) news-editorial/defamation posture **before publishing the FIRST auto-drafted Brief article**
   (drafting/queuing is fine); **(c) CCO gate before any user-facing credit-content notification
   path is enabled through the GIOS notification mechanism (ADR-0027 §5.5).**
4. **User-side Stripe emails:** enable Customer emails → Successful payments; merchant-notification.
5. **Owner test:** Brief comment flow (post → banned phrase → 422; report → moderate at
   `/admin/brief/comments`).
6. **Vercel Skew Protection** is BLOCKED on Hobby (`invalid_billing_plan`) — OWNER decision: upgrade
   to Pro (~$20/mo) to enable. Checklist in `OPERATIONS.md`.

## Known issues / debt
- Favicon/OG images still the pre-de-shadow shield render (on-brand; regen pending).
- `.env.example` drift: still lists deleted `SETUP_SECRET`; missing `COMPANY_POSTAL_ADDRESS`.
- `tsconfig.tsbuildinfo` tracked in git (build artifact) — confirm untracked.
- GIOS kernel debt (D-07/D-08 effect-safety, D-02 perf) → FOUNDER-CONTEXT §10.

## Operational runbooks
- **`OPERATIONS.md`** (root): deploy protocol · incident triage · CDN cache contract · rollback ·
  skew protection. Triage rule: `x-cv-release` header = 12-char build SHA; stale SHA = deploy skew
  (self-heals on refresh), current SHA = real bug. Codebase has **zero server actions** (all
  mutations = API routes) — never add one without skew protection.
- **Deploy:** push to `main` → Vercel auto-deploys prod (~2 min); **confirm with owner before
  pushing**. Env-only change → `npx vercel redeploy <prod-url>`. Build command lives in
  `vercel.json`. ⚠️ `prisma db push` silently fails through Accelerate → new tables self-heal at
  runtime (ADR-0001). Details: `CLAUDE.md` + `.ai/RUNBOOKS/deploy.md`.

## Next recommended tasks
1. **Recipient Intelligence v1** (owner-approved 2026-07-17, NOT started — begins after this push).
   **EXTEND, do not build a parallel engine.** Ground-truth inventory (12 areas, 3 independent
   adversarial lenses, all three returned `EXTEND_EXISTING`) found most of the proposed Recipient
   Intelligence Engine already ships: address extraction from reports exists end-to-end
   (`lib/aiParse.ts:74-85` 7 `furnisher*` fields, prompt rule 8 "do NOT invent or guess an address"
   → `lib/analyze.ts:120` → `saveFurnisherContact` → `TradelineContact`); auto-population exists
   (`app/api/letters/generate/route.ts:114-121` falls back to the parsed contact); and
   `app/api/mail/prepare/route.ts:38` already resolves from the STORE, never the textbox.
   **Two PROVEN defects are the real work:** (a) `saveFurnisherContact` has exactly ONE caller
   (the parser) — a user's typed correction is NEVER persisted, so mail/prepare still 422s them;
   (b) `validateAddress` (`lib/mail/MailProvider.ts:106`) has ZERO production callers — a live,
   typed, unwired port. Approved v1 scope: wire the persistence, add a `source` field
   (`credit_report` | `user_confirmed` | `bureau_reported` | `manual_override`), preserve multiple
   observations WITHOUT inventing reconciliation, confidence INTERNAL-ONLY. **Blocked/deferred:**
   "Verified by Kai" is a truth claim nothing substantiates (CASS deferred; `deliverable:true` is
   fabricated from a ZIP regex at `LetterStreamProvider.ts:76`) and would violate the letter
   engine's own Rule 2 (`lib/letter.ts:464`) — needs a real verification predicate + counsel
   sign-off. Cross-bureau reconciliation is speculative (the schema cannot represent two addresses;
   `TradelineContact` is 1:1 on `tradelineId`). A Recipient Graph would be the SECOND unwired
   consumer of `buildCreditGraph` — **`graphInputFromSnapshot` does not exist** (verified zero hits;
   the graph runs only on the synthetic fixture). Reuse `scoreConfidence`
   (`lib/intelligence/reasoning.ts:78`); do NOT mint a 7th confidence ladder.
2. **GIOS Sprint 3**: ADR-0028 + #11 Durable Audit → D-02 perf harness → route flips.
   Ranked plan: **FOUNDER-CONTEXT §11**.
2. Owner: unblock the digest (`COMPANY_POSTAL_ADDRESS`); run/confirm the encrypt backfills.
3. CreditVector app backlog: deferred perf items (Sidebar context provider, letters/upload
   server-prefetch); `#16` KaiAnswer store blocked on ADR-0006 founder approval.

## Validation status
`npx tsc --noEmit` clean · `next build` clean · guards green (kernel 33 · credit 19 · notify 23 ·
plus the app guard suite per `TESTING.md`). MAIL_LIVE OFF (no provider traffic possible).
