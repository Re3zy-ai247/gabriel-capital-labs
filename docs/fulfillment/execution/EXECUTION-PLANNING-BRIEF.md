# CreditVector Fulfillment Platform — Phase 1 Execution Planning Brief (Program Director)

Continuation. Architecture is ACCEPTED and LOCKED (do not reopen). This is PLANNING ONLY — no implementation, no coding, no merge, no deploy, no schema change. Every agent writes a planning document under `docs/fulfillment/execution/`. Reuse the approved architecture in `docs/fulfillment/` — delta planning, no rediscovery. Target public launch: **September 1, 2026**.

## Locked architecture (binding inputs — plan against these, never redesign)
CreditVector Fulfillment Platform · Case Journey Runtime · Dispute Package = primary object · provider-neutral · LetterStream = Provider Adapter #1 · Kai Runtime responsibilities · Operational Room Constitution · Fulfillment Policy Engine · Fulfillment Recovery Engine · **Provider Acceptance = irreversible boundary** (settled hold stays settled; post-acceptance remediation is accounting `adjust` only; never pretend the mailing didn't happen). Wallet mechanics per the re-gated commitment model (anchor lock, per-letter/attempt grain, unified attempt+1, canonical claim-key registry, nullable actor, MailManifestFlags additive migration, FINAL REVIEW pre-Submit token). Source docs: CREDITVECTOR-FULFILLMENT-ENGINE-V1, WALLET-COMMITMENT-MODEL, FULFILLMENT-COMMITMENT-BOUNDARY, RECOVERY-ENGINE, KAI-FULFILLMENT-UX, ADR-PROPOSALS (0041–0047), IMPLEMENTATION-SEQUENCE, COMMITMENT-RESOLUTION.

## New Founder decisions to fold in (treat as locked)
- **Vector Credits (VC) = the internal utility currency.** ONE Wallet, with SEPARATE append-only ledgers: **Purchased VC · Earned VC · Bonus VC · Pending Payout VC.** Each ledger is independent, append-only, fold-derived, `Restrict` FKs (XpAward precedent). The five-instruments-never-converted law (ADR-0038 PGE-3/4) still governs — no silent conversion between ledgers; Earned/Bonus/Pending-Payout touch reward/affiliate/cash-adjacent territory and are HEAVILY gated (counsel + CCO), planned but not implemented.
- **Case Journey Runtime = the primary operational workflow. Everything reports INTO the Journey** — not Mail, not Wallet, not Kai. Mail Center, Wallet, Kai, Timeline, Mission Control are participants/views of the Journey.

## Authoritative gates (planning must respect; do not plan past a closed gate)
1. **Gate D Phase −1** — must execute before ANY new migration (prod has no `_prisma_migrations` history; preflight rejects a 7th migration directory, ID-B02). This is the first buildable phase; ALL new schema queues behind it.
2. **Founder legal review** + **CROA §404 counsel review** — block ALL wallet money-movement implementation (prepaid funds received in advance; keeping the wallet does not moot the question; compliant alternative on file = Stripe manual-capture at delivery).
3. **LetterStream clarification questions** — the 11-question vendor-confirmation set must be answered before the live adapter wiring phase.
Wallet implementation stays BLOCKED behind gates 2. No plan may schedule wallet money code before that gate clears.

## Program Director sequencing rulings (seed the dependency graph; Agent A formalizes/refines)
- **R1 — The two-option law splits the critical path.** "Download Package" (self-mail) needs NO wallet and NO live provider; "Send with CreditVector Fulfillment" needs wallet + provider. Therefore the DOWNLOAD path is the first shippable fulfillment increment and can proceed while the wallet legal track runs in parallel. Sequence the wallet-free value first.
- **R2 — Gate D is the universal unblock.** Nothing with new schema starts until Gate D Phase −1 lands. The Case/DisputePackage/DisputePackageLetter/Claim/WalletLedger/MailManifestFlags migrations are additive and queue behind it in a dependency-correct order (Agent A defines).
- **R3 — Non-money fulfillment can precede money fulfillment.** State machine, provider abstraction (interface only), Recovery Engine skeleton, Mail Center read/evolution, and Case Journey read-model do not move money and are not CROA-blocked; they can build behind Gate D while wallet money-movement waits on legal.
- **R4 — Provider stays dry-run until vendor answers.** `MAIL_LIVE` off and the `not_wired` throw remain until the 11 vendor questions are answered AND the adapter conformance suite passes. No live LetterStream call is scheduled before that.
- **R5 — Everything ships flag-gated fail-closed** (`=== "true"` idiom, default OFF), additive-migration-first, with a rollback plan per phase. No build step mutates the DB.
- **R6 — Vendor Opacity is a build-time invariant**, not a late polish: operator-facing surfaces never carry provider identifiers from day one of the adapter work.

## Refined phase skeleton (Agent A refines against repo truth; the Founder's 10 phases are the starting frame)
P1 Gate D Phase −1 (execute) · P2 Wallet legal/CROA decision (parallel track, gates all money code) · P3 LetterStream conformance (vendor Q&A + adapter conformance suite, dry-run) · P4 Provider abstraction (interface + Vendor Opacity, no live wiring) · P5 Fulfillment Engine (Case/DisputePackage/state machine/Policy Engine/Recovery Engine — non-money) · P6 Mail Center evolution (Case Journey workspace; Download path first) · P7 Wallet Runtime + Vector Credits (AFTER P2 clears) · P8 Kai Fulfillment guidance · P9 Internal Founder testing · P10 Beta rollout. Insert the wallet-free "Download Package" milestone early (R1); keep all money-movement strictly behind P2.

## Agent assignments (each writes ONLY its own doc under docs/fulfillment/execution/; no commits; cite the source architecture docs; label PROPOSED / FOUNDER-GATE / VENDOR-CONFIRMATION-REQUIRED / LEGAL-GATE; include per-domain risks + tests + flags + migrations)
- **Agent A — `EXEC-SEQUENCING.md`:** build order, dependency graph (as a Mermaid graph + table), feature-flag order, migration order (additive, Gate-D-gated, dependency-correct), repository-impact map (which existing files each phase touches — reuse-first). Formalize R1–R6 into the phased roadmap; produce the master Dependency Graph, Implementation Sequence, Migration Plan, and Feature Flag Strategy. Every phase: entry criteria, exit criteria, gate dependencies, rollback note.
- **Agent B — `LETTERSTREAM-ADAPTER-PLAN.md`:** adapter boundaries against the existing `lib/mail/MailProvider` interface; endpoint / callback / tracking / error / provider-state / metadata INVENTORY as a CreditVector-concept mapping TEMPLATE (the repo has NO LetterStream API docs — do NOT invent endpoints; every vendor-specific cell is VENDOR-CONFIRMATION-REQUIRED, to be filled from the Founder-reviewed docs; the 11 vendor questions are the named gaps). Map all provider states → the canonical FulfillmentStage taxonomy; Vendor Opacity DTO discipline (never expose LetterStream terms to operators); the adapter conformance test strategy (dry-run first; sandbox→live gating; `MAIL_LIVE`/`not_wired` fail-closed preserved).
- **Agent C — `MAIL-CENTER-EVOLUTION-PLAN.md`:** how the existing `/mail` + send wizard evolve into the operational Fulfillment workspace under the Case Journey Runtime — REUSE-FIRST, no redesign (name every existing file reused/extended). The Download-Package (wallet-free) path first (R1); the Send path gated. Metrics→context; recommended-action band; per-package evidence + timeline; the FINAL REVIEW pre-Submit gate placement. Per-phase reuse map + what's genuinely new.
- **Agent D — `WALLET-VC-RUNTIME-PLAN.md`:** the Wallet Runtime execution plan (authorization / reservation / settlement / accounting / ledger) per the re-gated commitment model, PLUS Vector Credits: ONE wallet, four separate append-only ledgers (Purchased / Earned / Bonus / Pending Payout), each fold-derived with Restrict FKs; the ten inherited financial invariants; Marketplace / Growth Network / payout INTEGRATION POINTS (named only, separate instruments, counsel-gated). ALL money code explicitly LEGAL-GATE'd behind P2 — plan the sequence but mark the whole runtime blocked-until-legal. State exactly what is planned vs blocked.
- **Agent E — `CASE-JOURNEY-RUNTIME-PLAN.md`:** how Kai + Timeline + Fulfillment + Wallet + Mission Control converge into ONE deterministic Case Journey (the primary workflow everything reports into). The Journey as the top-level read-model/runtime; the others as participants/views; the deterministic event spine (KaiEvent + Event Bus contracts reused); the Next-Recommendation loop; how the Journey renders truthfully across the wallet-blocked interim (Download path live, Send path pending). Per-domain risks + tests.

After A–E: the Program Director synthesizes the top-level **EXECUTION-PLAN.md** (unified phased roadmap) + **TESTING-STRATEGY.md** + **ROLLBACK-STRATEGY.md** + **RISK-REGISTER.md** + Founder Summary, then ONE bounded Opus adversarial review of the SEQUENCE ONLY (not the architecture), then the mobile handoff (copy-paste report + gist + ZIP). Do not implement anything. Stop for Founder approval.
