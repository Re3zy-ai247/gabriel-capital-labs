# Phase 1A — Experience Runtime Implementation Brief (Program Director contract)

Founder-approved implementation. Branch `feat/experience-runtime-phase-1a` from `origin/main` `f449c35`. Serial bounded agents; each commits its own bounded work; the coordinator pushes. STOP after Phase 1A — no Wallet, no Money Runtime, no LetterStream, no continuation without explicit Founder authorization.

## Mission
When an operator logs in, CreditVector should already feel like **"The Credit Operating System."** Implement the Experience Runtime: Session Runtime · Mission Control as Executive Queue · Case Journey progression · Mail Center Download workflow · Kai context-aware experience — plus the approved live-defect fixes.

## HARD CONSTRAINTS (every agent, non-negotiable)
1. **ZERO schema changes.** No new tables/columns/enums/migrations, no self-heal DDL (`scripts/schema-safety.test.ts` must stay green). Gate D has not run. The Session object and Package grouping are **derived read-models** over existing data (KaiEvent, KaiSeen, Letter, MailManifest via PrismaMailStore, ProductEvent, User) — computed, never persisted. `sessionStorage`/derived caches allowed where precedent exists (scoped correctly).
2. **ZERO money / provider / CROA-sensitive expansion.** No Wallet, no Vector Credits, no LetterStream, no MAIL_LIVE, no settlement/payment, no provider adapter, no new mailing claims. The Send path stays exactly as shipped (dry-run wizard) and where the evolved UI references Send it is **visibly present, honestly disabled/unchanged** — never removed, never faked live.
3. **Compliance bar on every copy change** (`lib/compliance.ts` spirit): educational, no outcome promises, §611 anchored at RECEIPT, no fabricated telemetry/progress/urgency. The Download honesty fixes make copy MORE compliant; nothing may go the other way. All changed operator-facing copy is listed in your report for the CCO pass.
4. **Reuse-first.** Evolve existing files (`lib/kaiHome.ts`, `lib/mailCenter.ts`, `lib/missionControl.ts`, `app/dashboard`, `app/mail`, `app/journey`, `components/kai/*`, `components/mission/*`); new files only where the accepted plans name them. Match repo idiom; comments only for non-obvious constraints; smallest relevant diff.
5. **Validation before every commit:** `npm run typecheck` · `npx next lint --file <touched>` · `npx tsx scripts/schema-safety.test.ts` (17/17) · other guard scripts touching your area (see .ai/TESTING.md) · `npx next build` for structural changes. Commit only your listed files by explicit path; trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Never push.
6. **Zero-fabrication law.** Every rendered fact derives from real rows/events. "Visited" is never "completed". Placeholder stages stay honest.

## Specs of record (read-only, cross-worktree — the accepted design + simulation docs live on the docs branch)
`/Users/re3zy/Documents/worktrees/creditvector-fulfillment-engine-v1/docs/fulfillment/` — esp. `simulation/SIM-REVIEW.md` (the 14 experience findings + minimum set), `simulation/ROOM-RECOMMENDATIONS.md` (the 12 adjudicated recommendations), `simulation/JOURNEY-MAP.md`, `B-MAIL-CENTER-EVOLUTION.md`, `KAI-FULFILLMENT-UX.md` (voice law; Download-scope only), `execution/MAIL-CENTER-EVOLUTION-PLAN.md` (the schema-free acceleration slice), `execution/CASE-JOURNEY-RUNTIME-PLAN.md`, `OPERATIONAL-ROOM-CONSTITUTION-PROPOSAL.md`. Never modify that worktree.

## Agent scope + file ownership (serial: A → B → C → D → E)
- **A — Session Runtime.** NEW `lib/operatorSession.ts` (name avoids `lib/session.ts` auth collision): a pure derived read-model `buildOperatorSession(user)` → { identity (name, altitude: consumer|agency-owner|in-workspace(client)), yesterdayCompleted[] + todayCompleted[] (KaiEvent-derived, typed, capped), interruptedWork[] (resumable wizard/package states from server statuses), todaysPriorities[] (reusing pickRecommendation + roster ladder — do NOT fork the engines), sessionClose { doneToday, remaining } }. Zero AI, zero network, fully typed, unit-guard script `scripts/operator-session.test.ts` (static+logic checks, ≥12 checks). Wire NOTHING yet (B/E consume it). Also fix the greeting foundation: expose `greetingName` + `onBehalfOf` (workspace client) fields.
- **B — Mission Control.** `app/dashboard/page.tsx` + `lib/missionControl.ts` + `components/mission/*`: session-aware header (Kai greeting block consuming A: "Good morning, {name}." + yesterday-completed + today's priorities + "Continue where you left off" resuming interruptedWork), **altitude-aware**: `isAgency` + no workspace → Executive-Queue idiom over the roster's priority ladder (basis-carrying rows, not a bare sort) instead of consumer upload prompts (fixes the agency-owner confusion — zero `isAgency` in the room today); consumer view keeps the queue + adds session blocks; metrics demoted to context; session-close block ("what was done · what remains"). No new engines — compose kaiHome/ExecutionEngine/roster.
- **C — Case Journey.** (1) `lib/kaiHome.ts`: branch-5 candidate selection ordered by existing `score`/`probability` (orderBy or in-memory sort — no schema); starvation guard: the lapsed-window branch (and the verified-no-follow-up absorbing state) gains an aging qualifier so §605/time-discovered branches can surface (design per SIM-REVIEW finding 5 — deterministic, basis-carrying; document the rule in the code's own terse style). (2) Journey progression on the client view: current step → next step → Kai recommendation → timeline → evidence → waiting → next review (derived from the canonical map, Download-scoped; reuse `lib/mailCenter.ts` stages + `app/journey`). (3) Waiting-period truth distributed: the existing "Kai is watching the clock" line reaches the Mail Center row + Timeline states. (4) Timeline improvements per JOURNEY-MAP (honest placeholders unchanged).
- **D — Mail Center.** `app/mail/*` + `lib/mailCenter.ts` (+`app/letters/print/[id]/*`): the schema-free evolution over today's `Letter[]` — work queue grouped as derived Packages (letter-group by campaign/generation batch — derived, no table), ONE recommended-action band (defer to the same engine as Mission Control — no second ladder), evidence drawer (letters + self-attested mailing records; the Send-only artifacts truthfully absent), metrics→context strip, per-package timeline on the canonical stages (Download-scoped; Send stages present-disabled honestly). **Download Package workflow:** package-level review chain steps 1–7 (Kai Summary panel slot [E fills the content], letter+PDF preview, the Approve control OUTSIDE any Kai-labeled panel — fix the shipped conflation at app/mail/send/[letterId]/page.tsx:165-205 for the Download context) → package Download (aggregated print flow). **Download honesty triple:** §611 copy AND math receipt-anchored on the Download path (fix app/letters/print/[id]/page.tsx:153-154 and every mailedAt-anchored calc feeding operator copy — where receipt date is unknowable for self-mail, the copy states the estimate honestly: "the window starts when the bureau receives it — typically N days after mailing"; the math labels itself an estimate); evidence-asymmetry disclosure at the Download/Send fork; capture the ACTUAL mailing date at "mark mailed" (a prompt defaulting to today — existing field, no schema).
- **E — Kai Experience.** (1) Context-aware greeting + everyday on-behalf-of register (components/kai/* + the A session object): "Welcome back, Marcus — in Elena's workspace" idiom everywhere the static text sits today; KaiPresence exclusion list updated for the new session-aware Mission Control (no double presence). (2) **Cache fix:** scope `kai-presence-ctx-v1` per user+workspace or clear on workspace switch (components/kai/KaiPresence.tsx:18 + the switch paths). (3) **Emoji/range-law fix:** app/api/letters/[id]/round2/route.ts:40 — delete the 🎉, reword within the range law. (4) **Onboarding:** fix visited-as-completed semantics FIRST (derive real completion from the same signals the steps' target pages use — report-uploaded, letter-generated etc.; no fake progress), THEN wire the front door (signup redirect for brand-new accounts + a nav entry while incomplete). (5) Daily summary + work-completion narration (consuming A's sessionClose; kaiHome idiom, deterministic, basis-carrying; "quiet is allowed"). (6) Kai copy pass on touched surfaces: information-order (defect → correction), no new repetition; the 16-state emotional register respected.

## Out of scope (refuse even if convenient)
Anything in the DO-NOT list; cross-client recommendation unioning (post-P5 data); client-facing views; staff identity; FINAL REVIEW changes (Send-scoped); interim-sim items; any `.ai/` doc rewrites beyond CURRENT-STATE at the end (coordinator handles).
